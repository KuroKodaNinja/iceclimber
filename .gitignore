# Compiled binaries
/iceclimber
/popo          # stray from `go build ./cmd/popo` (the embedded bins live in internal/popobin/bin/)

# Local operator config and caches (may contain host/path details)
/iceclimber.yaml
/.iceclimber-cache/

# Demo state: generated config + the subscription token (never commit)
/.demo/

# Go build/test artifacts
*.test
*.out

# Release artifacts (make release)
/dist/

# Local symlink to the global washu guidance kit (machine-specific)
/.agents
/internal/popobin/bin/popo-*
