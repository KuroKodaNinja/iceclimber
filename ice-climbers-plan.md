# ice-climber — design document

Living design doc for `iceclimber`, a Go CLI for operating a Claude agent running in YOLO mode inside a sandbox it can't otherwise provision (Python, packages, web access), over an SSH-only link, with zero assumptions about the sandbox's shell or installed tooling.

Status: **v1 + post-v1 increments complete and verified end-to-end** against a real Alpine/musl sandbox (all §12 phases ✅, decisions #44–#61). Shipped on top of v1: **three languages** — Python/pip, JavaScript/npm, Java/Maven, each with runtime + packages over Tier 0 (mirror) and Tier 1 (relay) — and the **TUI-first operator console** (Phases 1+2). The pre-fleet hardening pass (#54) closed the console error-visibility gaps and the `sandbox_id` namespacing invariant is now tested. The `ExecFS` bulk-transfer (decision #55) shipped too — a runtime tree now pushes in one `tar` exec, so all three languages install over either transport (no SFTP-only constraint). `iceclimber trust` (decision #56) brings SSH host-key recording in-CLI (with a console first-connect modal) so onboarding an ephemeral sandbox no longer needs an out-of-band `ssh-keyscan`. `iceclimber agent install claude` (decision #57) relays a coding agent + its subscription auth into the (air-gapped) sandbox and drops a `$ICECLIMBER_HOME/nana` launcher (#58) the operator runs to start it, NANA.md wired in as system context. The demo dogfoods `nana`. (Also fixed: `web.fetch` blobs at the spec'd `protocol/blobs/` — #59.) The agent now talks to Popo via a relayed `popo` client (decision #61) that owns the protocol ceremony, shrinking NANA.md to a minimal system-prompt contract (file-I/O-only preserved as a PROTOCOL.md fallback). Remaining work: **multi-sandbox** (the fleet — Phase 3) and the v2 backlog (sub-agent/`web.research`, Tier 2 build). No open v1 polish items.

---

## 0. Scope — the v1 line

The design below is kept in full; it is **not** all v1. To honor "ship the minimum that solves the problem," v1 is cut as tightly as it can be while still being a real tool, and the rest is parked as **v2** — designed enough not to be re-derived later, explicitly out of the first build. Every parked item names its own re-entry trigger ("build this when X actually happens"), so v2 is demand-driven, not speculative.

**In v1 (the tight cut):**

- `probe` + `bootstrap` — fingerprint, install-root selection, tree creation, smoke test
- Maildir protocol end-to-end: `ping`, `python.install`, `pip.install`, `web.fetch`
- `RemoteFS` with `SFTPFS` + `ExecFS` and the conformance suite
- `python.install` via python-build-standalone (absolute-path contract)
- `pip.install` Tier 0 (mirror, remote-exec) and Tier 1 (relay)
- `web.fetch` with venue routing, the SSRF floor, **egress gating + fetch rewrites (§6.1)**, audit log
- `NANA.md`
- *(post-v1)* acceptance demo (phase 8, [`DEMO.md`](./DEMO.md)) + **observability**: a host activity log and `iceclimber logs` two-tail (`[POPO]`/`[NANA]`) viewer

**Parked for v2 (thinking preserved in place, marked `v2:`):**

| Parked | Why not v1 | Re-entry trigger |
|---|---|---|
| Tier 2 build-on-controller (§5) | Reimplements cibuildwheel for the least-likely path; most packages ship manylinux wheels | First real package with no wheel anywhere *and* not in the mirror. v1 fails loudly: "add it to your mirror." |
| `web.research` sub-agent + Tier 3 (§4.5) | The sandbox is already a Claude agent; this is a second product | A concrete resolution case where surfacing to the operator proves insufficient |
| Fleet design (§8) | "Design for it now" is the unrequested-flexibility anti-pattern | A second sandbox that one-process-per-sandbox can't serve. v1 keeps only `sandbox_id` namespacing. |
| ~~`ExecFS` bulk-transfer wire protocol (§6)~~ | ✅ **Shipped post-v1 (decision #55)** — not a custom wire protocol but `tar -xf -` over one exec; closed the SFTP-only-for-runtime-push constraint. | — |

---

## 1. Naming

One Go binary, two roles, named after the two Ice Climbers:

- **Popo** — the controller. Runs *outside* the sandbox (confirmed: typically on a laptop, fully outside the corporate network the sandbox sits in). Owns the SSH/SFTP connection, the deterministic dispatcher, the sub-agent fallback, the local package cache.
- **Nana** — the sandbox-side role. Not necessarily a running process — primarily a persona + skill document (`NANA.md`) that the Claude agent follows using whatever tools its own harness already gives it. An *optional* helper binary (`iceclimber nana ...`) can be pushed in as a convenience once Popo confirms the sandbox can execute one, but nothing in the system depends on it existing.

Functional CLI verbs stay plain and unambiguous (`bootstrap`, `serve`, `install`, ...). The Popo/Nana flavor lives in log lines and status output, not in command names.

---

## 2. Architecture overview

```
 Public internet                              Internal mirror (Artifactory/Nexus/etc.)
        |                                                    |
        v                                                    v
+--------------------------+                    +--------------------------+
| Popo (controller)        |   SSH / SFTP        | Nana (sandbox)           |
|  - package cache         | <==== requests ==== |  - outbox / inbox        |
|  - sub-agent fallback    | ====  results  ====>|    (maildir-style)       |
+--------------------------+                    |  - runtime tree           |
                                                  +--------------------------+
```

- Popo can reach the public internet but **not** the internal mirror (it sits outside the corporate network).
- The sandbox can reach the internal mirror and other company-approved endpoints directly, but has no general internet egress and no way to fetch/install anything itself.
- Requests flow sandbox → controller via `outbox`; results flow controller → sandbox via `inbox`. Both directions ride the same SSH connection.

### Key architectural decision: no daemon required inside the sandbox

The protocol only requires Nana to have *some* file read/write capability — true of essentially any code-capable agent harness. Popo does all polling via its own SSH session (SFTP or exec, see §6). A binary inside the sandbox is a convenience, never load-bearing.

**Scoping boundary (explicit):** actually *running* installed software still requires Nana's harness to expose some execution primitive that resolves paths against the same filesystem Popo populates. If a harness only exposes a fully isolated "run code" tool unconnected to that filesystem, no installation strategy on Popo's side can fix that — that's a harness limitation outside this project's scope, not a bug to work around.

### Not a real chroot

No privileged operations are assumed. The "chrootable" instinct is implemented as a self-contained, relocatable prefix tree (closer to Homebrew/pyenv than `chroot(2)`), addressed by **absolute paths**, not PATH/profile-file modification — non-interactive exec per tool-call means we can't rely on `.bashrc`/`.profile` being sourced, and we don't know which shell (if any specific one) is in play.

---

## 3. Directory layout

### Sandbox side (`$ICECLIMBER_HOME`, chosen during bootstrap, see §7)

```
$ICECLIMBER_HOME/
  protocol/
    outbox/
      tmp/            # Nana writes here first
      new/            # atomic rename in — Popo watches this
      cur/            # Popo renames here on pickup (rename = lock)
    inbox/
      tmp/            # Popo writes here first
      new/            # atomic rename in — Nana watches this
      cur/            # Nana renames here after consuming
    blobs/
      <sha256>        # content-addressed: wheels, fetch bodies, python tarball contents
    heartbeat         # Popo writes current timestamp as CONTENT (not mtime — see §6)
    capabilities.json # sandbox self-report, CONTROLLER-written (host + agent identity; see decision #72)
    .popo.lock        # held by `serve` for the duration of the session
  popo                # the sandbox client (relayed at bootstrap; `popo <verb>` — #61)
  runtimes/
    python/
      3.12.6-x86_64-glibc/
        bin/python3   # absolute-path contract — canonical address for invocation
  state/
    manifest.json     # convenience copy; Popo's local copy is authoritative
    pip.conf          # generated at bootstrap, index-url -> internal mirror if configured
  skill/
    NANA.md           # minimal contract (defaults the agent to `popo`)
    PROTOCOL.md       # raw file protocol — the file-I/O-only fallback (no-exec harnesses)
```

Request/response filenames are `<ulid>.json` — ULIDs sort lexically by creation time, so "what's oldest in the queue" is a plain directory listing, no per-file parsing required.

### Controller side (operator-owned, never written by Nana)

```yaml
# iceclimber.yaml
sandbox_id: my-sandbox-1
ssh: { host: ..., user: ..., identity_file: ... }
remote_root: /home/agent/.iceclimber     # confirmed/auto-set during probe

network:
  allowed_domains:
    - pattern: "artifactory.corp.internal"
      reachable_from: sandbox
      role: package_mirror               # -> pip.conf index-url points here
    - pattern: "docs.corp.internal"
      reachable_from: sandbox
  # Controller-venue fetches are gated, not allow_and_log (§6.1, supersedes decision #7):
  #   no match in the persistent allow-list -> held in `pending` for operator approval.
  # Sandbox-venue and rewritten-to-mirror fetches ride approved egress and are never held.
  unlisted_domain_policy: gate            # hard SSRF floor below is non-configurable regardless

fetch_rewrites: []                        # §6.1 — redirect/re-venue table (e.g. Maven Central -> Artifactory)

cache_dir: ~/.iceclimber-cache            # wheel/runtime cache, shared across sandboxes by platform fingerprint
approvals_file: ~/.iceclimber/approvals.json  # operator-owned persistent allow-list; never Nana-writable
```

`network.allowed_domains` is a **routing table**, not an access grant — the actual network boundary is enforced outside this tool. It tells Popo (a) which venue to use for a fetch (§6) and (b) whether an endpoint is the package mirror.

A hard-coded floor — blocking link-local/metadata-style addresses (e.g. `169.254.169.254`) and other obvious SSRF/lateral-movement targets — sits underneath `unlisted_domain_policy` and is **not** a config toggle. "Allow and log" governs arbitrary public domains, not the cloud's own metadata service.

---

## 4. Protocol — envelope and schemas

### Envelope

```jsonc
// outbox/new/<ulid>.json (request)
{
  "schema_version": 1,
  "id": "01J9XQK...",
  "type": "pip.install",
  "created_at": "2026-06-21T18:32:00Z",
  "params": { /* type-specific */ }
}
```

```jsonc
// inbox/new/<ulid>.json (response, same id)
{
  "schema_version": 1,
  "id": "01J9XQK...",
  "status": "ok",                 // ok | error | needs_clarification
  "completed_at": "2026-06-21T18:32:04Z",
  "result": { /* present if status=ok */ },
  "error": { "code": "...", "message": "...", "retryable": false },
  "clarification": { "question": "..." }
}
```

`status` describes whether Popo *successfully serviced the request*, not whether every sub-item inside it succeeded — e.g. a `pip.install` batch where 2 of 5 packages fail is still `status: ok`, with per-package failures inside `result`. `status: error` is reserved for Popo failing to even attempt it (malformed request, missing target runtime, internal exception).

**No `pending` stub is written.** (Decided: heartbeat-only liveness, see §4.7 — simpler, revisit only if it proves confusing in practice.) Absence of a response file means "still in progress." Nana polls with backoff and separately watches heartbeat staleness.

**Delivery semantics — at-least-once, deduped to effectively-once (v1).** The transport can drop or double-deliver, so:

- **Crash recovery.** The `new → cur` rename is the pickup-lock, but a request sitting in `cur/` when Popo dies (picked up, no response written) is orphaned. `serve` sweeps `cur/` on startup: any entry without a matching response in `inbox/` is re-processed (or written a `status: error` if non-idempotent — see below).
- **Dedup by `id`.** If an inbox response is lost and Nana re-submits the same logical request, Popo dedups on the envelope `id`: a request whose `id` already has a durable response replays that response instead of re-executing. `python.install`/`pip.install` are naturally idempotent; **`web.fetch` with a non-GET method is not** — for those, re-execution on an unconfirmed delivery is unsafe, so a recovered-from-`cur/` non-idempotent request is answered `status: error, code: interrupted_unsafe_retry` rather than silently re-POSTing.
- Responses are written durably (tmp-write + atomic rename, §6) so a half-written response never looks complete.

### 4.1 `ping`
```jsonc
params: {}
result: { "pong_at": "...", "popo_version": "0.1.0" }
```

### 4.2 `python.install` — *implemented (phase 3)*
```jsonc
params: { "version": "3.12" }   // minor version; Popo pins the exact patch
result: { "version": "3.12.13", "path": "/abs/.../bin/python3", "already_installed": false }
```
Popo resolves the exact patch from PBS's `latest-release.json` (tag + `asset_url_prefix`) and the release `SHA256SUMS` — picks the highest-patch `install_only` asset for the sandbox's `<arch>-unknown-linux-<gnu|musl>` triple, verifies its SHA256, and records the pinned version. The tree is extracted with the Go stdlib (no remote `tar`) and pushed over `RemoteFS` (either transport); `bin/python3` is then run over the exec channel to prove it executes (§2 boundary). Verified on Alpine/musl.

### 4.3 `pip.install` (batched) — *Tier 0 implemented (phase 4)*
```jsonc
params: {
  "python_version": "3.12",
  "packages": [{ "name": "requests", "version": "2.32.3" },  // pinned
               { "name": "rich" }]                            // unversioned -> native default
}
result: {
  "installed": [{ "name": "requests", "version": "2.32.3", "tier": "mirror", "sha256": "..." }],
  "failed": [{ "name": "foo", "version": "1.0", "error": "..." }]
}
```
`tier` ∈ `mirror | relay | built | subagent` — the audit trail (§5). **Resolve → retrieve (decision #21):** Popo first co-resolves the whole request against the index (`pip install --dry-run --report`) — native fidelity, exactly as installing a repo's requirements; an unsatisfiable graph fails the request (`resolution_failed`). It then installs each resolved package independently (`--no-deps`) so individual pull failures are per-package. **Versions need not be pinned in the *request*** (unversioned resolves by the manager's default) — but the *resolution* is always recorded with exact versions + sha256, so determinism lives at the resolved layer. (Supersedes the old "always pinned request" stance.)

### 4.4 `web.fetch`
```jsonc
params: { "url": "https://...", "method": "GET", "headers": {}, "body": null }
result: {
  "status_code": 200, "headers": {},
  "venue": "sandbox-exec",          // or "controller" — which side actually made the call
  "encoding": "utf8",
  "body_inline": "...",             // present if under ~16KB
  "body_blob": "blobs/<sha256>"     // present otherwise
}
```
See §6 for venue selection logic. **Phase 6a (implemented):** the **sandbox-exec
venue** — Popo runs curl, or busybox `wget`, *inside* the sandbox over the exec
channel (no Python; web.fetch is language-agnostic). Body returns inline
(utf8/base64) under 16 KB, else as `blobs/<sha256>`. The **controller venue** and
its policy layer (SSRF DNS floor, egress gating, fetch rewrites, pending/approve/
deny) are **phase 6b**.

### 4.5 `web.research` (sub-agent path) — `v2:` parked (§0)

> **v2.** Not in the first build — the thing in the sandbox is already a Claude agent, so a second research agent inside Popo is a separate product. v1 surfaces resolution ambiguity to the operator instead. Schema kept so v2 doesn't re-derive it.
```jsonc
params: { "question": "...", "context": "...", "max_iterations": 5 }
result: { "answer": "...", "sources": [{ "url": "...", "note": "..." }], "iterations_used": 3 }
```
**OPEN** — concrete tool loop, stopping criteria, prompt construction. See §10.

### 4.6 `capabilities.json` (self-reported by Nana once, not a queued request) — *superseded by decision #72*

> **Superseded (Phase 4, #72):** the schema below (`{has_exec, has_file_write}`,
> Nana-written) shipped only as a placeholder no production code wrote. It is now a
> CONTROLLER-written self-report — host facts (bootstrap) + the installed agent's
> identity (install/wrap) — and Nana writes nothing. The original §4.6 design is kept
> below for history.

```jsonc
// v1 shape — trimmed to fields something actually consumes (decision #14):
{ "has_exec": true, "has_file_write": true }
```
`has_exec` is the **viability gate**: only Nana knows whether its harness exposes an execution primitive that resolves paths against the tree Popo populates (§2 scoping boundary) — Popo's own SSH exec can't answer that. `has_file_write` is required to participate in the protocol at all. Dropped from v1: `has_network_tools` (Popo drives all fetches, §6) and `shell_hint` (ExecFS is pinned to POSIX sh, §6 — nothing branches on it). Re-add only when a consumer appears. *Phase 7: `NANA.md` instructs Nana to write this as its first action, and `status` **reads it where present** — Popo never requires it.*

### 4.7 Liveness — heartbeat by content, not mtime

Popo writes the heartbeat file's **content** (not mtime). This avoids needing any `stat`-equivalent on Nana's side (a portability trap under `ExecFS`, see §6) and works regardless of what Nana's harness tools expose — "read this file" is the only primitive required.

**Content is `<seq> <iso8601>` — a monotonic counter first, timestamp second.** Nana judges liveness primarily on **counter advancement** ("has `seq` increased across my last K polls"), which needs *no* clock synchronization between the two sides. Agent harnesses frequently have skewed or unreliable clocks, so comparing Popo's absolute timestamp against Nana's wall clock would produce false "Popo is down" verdicts. The timestamp is kept for human logs only, not for the liveness decision.

Skill-documented polling schedule: 2s / 5s / 10s / 30s backoff; if `seq` has not advanced across ~the last several polls (≈2 minutes of wall time at the tail of the backoff), stop waiting and surface "Popo appears to be down" rather than polling forever.

---

## 5. Package resolution tiers

Ordered by how much machinery each needs — always try the cheapest first:

- **Tier 0 — internal mirror, direct remote-exec.** *Implemented (phase 4).* Primary path. Popo remote-execs pip *inside* the sandbox against the mirror, using the sandbox's own already-approved egress — resolve (`--dry-run --report`) then per-package `--no-deps` install. No wheel transfer, no relay. Popo's commands pass `--index-url` **explicitly** (robust under non-interactive exec, decision #23); `bootstrap` *also* writes `state/pip.conf` so the agent's ad-hoc pip hits the same mirror. *(Tested against real PyPI as a stand-in mirror; the egress restriction itself is not yet modeled.)*
- **Tier 1 — Popo-side fetch + relay.** *Implemented (phase 5).* For packages the sandbox can't reach. Popo runs the **operator's `python3`** to `pip download --only-binary=:all: --platform <musllinux/manylinux tag(s)> --abi cp<ver> --python-version <ver>` (cross-platform, targeting the probed fingerprint), relays the wheels in via `RemoteFS` to `blobs/wheels-<id>/`, then remote-execs `pip install --no-index --find-links=… --report` in the sandbox (tier=relay, sha256 hashed from the wheels). The foreign-tag risk was **spiked and resolved** (musl-aarch64 wheels download cleanly from macOS, incl. C-extensions). Selected via `--tier relay` or `auto` when no mirror is set (decision #24–25). Sdist-only packages have no wheel ⇒ Tier 2 build (parked v2).
- **Tier 2 — build-on-controller fallback.** `v2:` parked (§0). Compiled extension, no matching wheel anywhere. Popo builds it in a container/VM matching the probed fingerprint, then drops into Tier 1's transfer mechanics. **OPEN** — exact container strategy. *v1 behavior: fail loudly ("no wheel anywhere; add it to your mirror"), don't build.*
- **Tier 3 — sub-agent.** `v2:` parked (§0) — see §4.5. Genuinely ambiguous resolution, or recovery from a Tier 0–2 failure.

Python itself is always relay-based (Tier 1-style transfer) — nothing exists in the sandbox yet that could pull it from anywhere, mirror or not. *Implemented in phase 3.* **Shebang note (decision #20):** PBS's `bin/python3` is relocatable and runs by absolute path with no shebang reliance, so phase 3 needs no rewriting. `pip`-installed console scripts *do* bake an absolute shebang at the install path — that bites in **phase 4** (pip), where entry points get shebang rewriting or `python3 -m` invocation. Deferred there, not here.

**Determinism note:** regardless of tier, the resolved package hash is always recorded in the response (§4.3) — "company-approved" doesn't mean unverified.

---

## 6. Network — fetch venues and transport

### Two fetch venues

Because Popo sits *outside* the corporate network and the sandbox sits *inside* it, fetches need two execution venues, chosen automatically per target rather than guessed:

- **Controller-side**: Popo fetches from its own network. Right for general internet / public resources.
- **Sandbox-side (remote-exec)**: Popo uses its existing SSH exec access to issue the fetch *from inside the sandbox's network position*, then relays the result back through the inbox file. Required for anything only reachable from where the sandbox sits (the internal mirror, internal docs, etc.) — and Popo can do this without Nana's own harness having any network tool, since Popo is driving the SSH exec channel directly.

Sandbox-side fetches use **curl when present, else busybox `wget`** (which does HTTPS, `--header`, `-O`, and `-S` for the status line) — **not** Python (decision #28; web.fetch must not be tied to the Python runtime). A box with neither curl nor wget is reported clearly.

### 6.1 Egress gating & fetch rewrites (v1) — *implemented (phase 6b)*

*Realized in `internal/egress` (policy + rule/pending stores) + `internal/webfetch` (controller venue, SSRF-safe dial) + the `pending`/`approve`/`deny` CLI. Plain `approve <id>` grants host scope (`--remember` for a custom glob); unlisted URLs default to controller-gated; `deny` persists a deny rule; the SSRF floor blocks private/link-local/metadata at dial (rebinding-resistant) and refuses literal blocked IPs up front.*

`web.fetch` via the **controller venue** is a deliberate tunnel through the sandbox's egress isolation: it lets an in-sandbox agent reach the public internet — including arbitrary POST bodies — from Popo's network position. That is the *point* (the sandbox can't egress on its own), but it is also a data-exfiltration path that defeats the sandbox's reason to exist, so it is **gated, not free**. The SSRF floor alone (blocking `169.254.169.254` and friends) does not address this — "the agent can `POST` whatever it scraped to an arbitrary public host" is the larger threat, and it is what the gate is for.

Two mechanisms run **in order**, before any controller-venue fetch:

**1. Rewrite table — redirect/re-venue before gating.** Generalizes pip.conf's `index-url`: a naive public URL is mapped onto the internal mirror that actually serves it, and re-tagged to the venue that can reach it. A request that rewrites onto a sandbox-reachable mirror **needs no approval** — it never leaves the approved network.

```yaml
# iceclimber.yaml
fetch_rewrites:
  - match: "https://repo1.maven.org/maven2/*"
    rewrite_to: "https://artifactory.corp.internal/maven-central/*"
    venue: sandbox            # rewritten target rides the sandbox's already-approved egress
  - match: "https://pypi.org/*"
    rewrite_to: "https://artifactory.corp.internal/pypi/*"
    venue: sandbox
```

Rewrites are operator-owned config (never Nana-writable). Matching is prefix/glob; the trailing `*` captures the path tail and is appended to `rewrite_to`. The audit entry records both the original and rewritten URL so the redirect is never silent.

**2. Approval gate — for fetches that survive rewriting and still aim at the public internet.** Checked against the operator's **persistent allow-list**:

- **match → allow**, audited.
- **no match → held**: the response is `status: needs_clarification`; the request lands in `pending`; Nana polls/backs off (§4.7). The operator then runs:
  - `approve <id>` — one-shot, this request only.
  - `approve <id> --remember <pattern>` — **persists** an allow rule (e.g. `https://docs.python.org/*`); all future matching controller-venue fetches auto-allow. This is the "permanent approval on Popo's side."
  - `deny <id> --reason "..."` — Nana gets `status: error`, `code: egress_denied`.

The allow-list and rewrite table live in **operator-owned state**, never writable by Nana. Sandbox-venue fetches and rewritten-to-mirror fetches are **never held** — they ride approved egress.

This is the concrete trigger §10 flagged as missing: **approval gates controller-venue egress, not "unlisted domains."** It supersedes the old `allow_and_log` default for the controller venue (decision #7).

**Per-verb kill switch (coarse control).** `serve --deny web.fetch` disables the verb entirely. The verb allowlist Popo serves *is* the security boundary: any file in `outbox/new` is treated as fully operator-authorized (file presence is the only authentication — acceptable for a single-operator laptop tool, stated explicitly so it isn't assumed otherwise). The SSRF floor sits underneath everything and no rewrite or allow rule can reach a link-local/metadata target.

### Transport abstraction: `RemoteFS` — *implemented (phase 2)*

```go
// internal/remotefs — all paths absolute; missing path -> errors.Is(fs.ErrNotExist)
type FS interface {
    Mkdir(ctx, path string) error            // mkdir -p / MkdirAll
    WriteFile(ctx, path string, data []byte) error
    ReadFile(ctx, path string) ([]byte, error)
    List(ctx, dir string) ([]string, error)   // sorted basenames; empty dir -> ([],nil)
    Rename(ctx, old, new string) error          // POSIX replace semantics
}
```

*Refined from the original `WriteAtomic(dir,filename,io.Reader)` sketch (decision #16):* the FS exposes primitive `WriteFile` + `Rename`, and the **maildir layer composes atomic delivery** (write to `tmp/`, rename into `new/`) so readers of `new/` never see a partial file. `data` is `[]byte` for now; a streaming `io.Reader` variant arrives with blobs (phase 3+).

Two implementations, chosen at session open based on whether the SFTP subsystem is actually available (it's sometimes disabled even when exec works); `--transport auto|sftp|exec` overrides (the override exists so the functional suite can exercise ExecFS even where SFTP works):

- **`SFTPFS`** — the fast path (`pkg/sftp`; `PosixRename` for atomic replace).
- **`ExecFS`** — built from day one (decided in review, not deferred), for sandboxes with the SFTP subsystem disabled.

A single conformance suite (`remotefstest.RunConformance`) runs against both — locally (host shell + in-process `net.Pipe` SFTP) **and** against the real Alpine VM over both SSH channels — asserting identical behavior (write/read roundtrip incl. NUL bytes, empty-directory handling, missing-path `ErrNotExist`, rename replace+source-gone). Nothing above this layer (probe, bootstrap, the dispatcher) ever knows which implementation is active.

**`ExecFS` command palette (pinned, not allowed to grow ad hoc):** `sh`, `mkdir -p`, `cat`, `mv`, `rm -f`/`rm -rf`, `ls -1`, `wc -c`, `chmod` (octal), `ln -s`, shell redirection, `&&`. No GNU-specific flags, no `stat` (flags differ across GNU/BSD/busybox). `chmod`/`ln -s` were added in phase 3 for the executable bit + symlinks a relocatable interpreter needs (decision #19); `RemoveAll` (`rm -rf`) added in phase 5 (decision #27). A sandbox lacking even this set is out of scope — stated explicitly rather than discovered later.

**Raw streaming over base64.** A non-pty exec channel is a clean byte stream — `cat > path` with content piped directly into the channel's stdin avoids ~33% base64 overhead. Never request a pty (ptys are what mangle bytes). Base64 kept in reserve only as a defensive fallback if some sandbox proves to mangle raw streams.

**Bulk transfer under `ExecFS`.** ✅ **Done (decision #55).** Naively opening one SSH channel per file (a runtime distribution is thousands of files) is slow — each channel open is its own round trip. The original sketch was a custom length-prefixed wire protocol; the shipped solution is simpler and needs no new protocol: a runtime push goes through `remotefs.PushTarGz`, and any `FS` that implements the `TreePusher` capability (`ExecFS` does) receives the whole tree as **one** `tar -xf - -C <target>` exec, the tar streamed into stdin. Gunzip and strip-leading-component are done controller-side in Go (re-taring on the fly) so the sandbox `tar` needs no `-z`/`--strip-components` — BusyBox `tar` suffices and preserves mode bits + symlinks. A plain `FS` (no `TreePusher`) still falls back to per-file `WriteFile`/`Chmod`/`Symlink`. This removed the v1 "**requires `SFTPFS` for `python.install`**" constraint — every language installs over either transport now, verified by `TestPythonInstallOverExec` on the BusyBox sandbox.

**Extraction trick (both transports).** Extract the Python distribution archive **on the controller** (which definitely has tar/zstd) and push the already-extracted directory tree file-by-file, rather than requiring the sandbox to have `tar`/`zstd`/`unxz` itself. A remote-tar fast path can be added later purely as a speed optimization once it's known to matter, never as a requirement.

### Security floor (not configurable)

- Hard block on link-local/metadata-range addresses and obvious internal-pivot targets, regardless of `unlisted_domain_policy`.
- Every `web.fetch`/`web.research` call gets an append-only audit entry: URL, venue, status/size, timestamp. Bodies are hashed rather than stored in full. *Resolved (phase 6a, decision #30):* **controller-side** JSONL (Popo's copy is authoritative, §3), one file per `sandbox_id`, schema `{ts, id, type, url, method, venue, status_code, body_size, body_sha256, outcome}`. Retention is unbounded for now (append-only); rotation is future work.

---

## 7. Bootstrap & probe sequence

1. **Connect & verify channels.** SSH connect, then verify `exec` and `sftp` independently — don't assume one implies the other. If `exec` fails, bootstrap fails immediately; if `sftp` fails, fall back to `ExecFS` (§6).
2. **Fingerprint the sandbox.** OS, arch, libc (multiple independent signals — disagreement ⇒ low confidence), free disk space, an actual write test (not just permission bits) at each install-root candidate, detection of an existing iceclimber tree (idempotent re-bootstrap). Every probe command is plain POSIX `sh` — no bashisms. Low-confidence fields pause bootstrap and ask the operator to confirm rather than guessing.
3. **Choose the install root.** First writable, durable candidate wins (operator-supplied path checked first, then `$HOME/.iceclimber`, then `/opt/iceclimber` as a root-only long shot). **Decided:** if none is writable, bootstrap fails and requires an explicit operator-supplied path — `/tmp` is never used as a silent fallback, since installs that vanish on reboot defeat the point.
4. **Create the tree & drop the skill.** Maildirs, `blobs/`, `runtimes/`, `state/`, `pip.conf` (safe to write before Python exists). `NANA.md` is written into the tree regardless of harness; the operator gets a printed reminder that wiring it into their specific harness's instructions is a manual integration step Popo can't auto-detect.
5. **Smoke test, no agent involved.** Popo writes a synthetic `ping` directly into `outbox/new` itself, runs one dispatch cycle, confirms `pong` lands in `inbox/new`. Isolates "is the plumbing broken" from "is the agent using it correctly."
6. **Report to operator.** Fingerprint, chosen root, channel capabilities, smoke-test result, skill path, any low-confidence flags. Refuses to proceed to `serve` if critical capabilities (exec) are missing.

`probe` (read-only, phases 1-3 minus writes) is a separate command from `bootstrap` (full idempotent setup) — useful as a standalone diagnostic ("is the box still reachable, has disk filled up") without touching anything.

---

## 8. Fleet extensibility — `v2:` parked (§0)

> **v2.** "Design for it now, build for one" is exactly the unrequested-flexibility the general principles warn against. v1 keeps **only** the `sandbox_id` namespacing below (cheap, one directory level) and the cache-by-fingerprint key; it does **not** build worker-pool or multiplexing machinery. v1's fleet story is literally "run N independent `serve --sandbox X` processes," which needs no fleet design — just no global singletons. The rest is preserved here for when a second sandbox actually arrives.

- All of Popo's local state (probe results, request/response logs, in-flight tracking) is namespaced by `sandbox_id`, even with only one configured today.
- The package/wheel **cache** is namespaced differently — by platform fingerprint (arch/libc/python version), not sandbox — since that's what's actually shareable across a future fleet.
- The `serve` loop is one worker per SSH connection, so "N sandboxes" later is "spawn N workers," not a rewrite.
- v1 fleet story: run N independent `iceclimber serve --sandbox X` processes side by side. A true multiplexed daemon is optional future work, not required now.

---

## 9. CLI command surface

```
iceclimber                               # (no subcommand) → the operator console:
                                         #   serve embedded in a TUI, live activity, inline approvals (#46)
  init                                   # scaffold iceclimber.yaml
  trust [--yes|--fingerprint SHA|--replace] [--sandbox ID]  # record the sandbox's SSH host key (in-CLI ssh-keyscan, #56)
  bootstrap [--sandbox ID] [--force]     # full idempotent setup; --force is destructive, needs confirmation
  probe [--sandbox ID]                   # read-only diagnostic
  serve [--sandbox ID] [--yes|--supervise]  # headless watch loop (supervised on a TTY)
  status [--sandbox ID]                  # heartbeat age, queue depth, cache size, recent requests

  install python <version> [--sandbox ID]
  install pip <pkg>[==version]... [--python VER] [--tier auto|mirror|relay] [--sandbox ID]
  install node <version> [--sandbox ID]                      # (#44)
  install npm <pkg>[@version]... [--tier auto|mirror|relay] [--sandbox ID]   # (#44)
  install java <version> [--sandbox ID]                      # Temurin JDK (#51)
  install maven <group:artifact:version>... --java VER [--tier auto|mirror|relay] [--mirror URL]   # JVM deps via Coursier (#52)

  agent install [claude] [--token-file F] [--skip-auth] [--sandbox ID]   # relay a coding agent + auth into the sandbox (#57)
  agent list                             # the agents iceclimber can install
  # (agent install also drops $ICECLIMBER_HOME/nana in the sandbox — operator runs `nana [agent] [-- args]`
  #  there to start the agent, auth + NANA.md system context wired in (#58); not an iceclimber subcommand)

  web fetch <url> [--method M] [--venue V] [--sandbox ID]    # gated egress (§6.1)

  logs [--sandbox ID] [--follow] [--agent-log FILE] [--type TYPE]
  tui  [--sandbox ID] [--agent-log FILE] [--snapshot]        # passive attach dashboard (#45)
  pending [--sandbox ID]                 # controller-venue fetches held for egress approval (§6.1)
  approve <id> [--remember <pattern>] [--sandbox ID]   # --remember persists an allow rule
  deny <id> --reason "..." [--sandbox ID]

  cache list | prune | gc                # (stub; v2)
  skill print [--protocol] | path [--protocol]   # NANA.md / PROTOCOL.md
  config show | validate

  # Sandbox-side (relayed at bootstrap, run by the agent INSIDE the sandbox — not iceclimber subcommands):
  #   $ICECLIMBER_HOME/popo <verb> [args]             # the client: build+deliver+poll+parse a request (#61)
  #   $ICECLIMBER_HOME/popo help                      # list verbs/usage
  #   $ICECLIMBER_HOME/nana [agent] [-- args]         # the launcher: start an installed agent, NANA.md as system context (#58)

  version
```

Global flags: `--config`, `--sandbox`, `--json`, `-v`.

**`install` reuses `serve`'s handler functions directly** (same tiering logic, called synchronously instead of triggered by a file in outbox) rather than going through the maildir round-trip.

**Decided:** `install` and `serve` contend for the same `.popo.lock`. If `serve` holds it, `install` fails fast with a clear message naming the sandbox and `serve`'s PID, rather than building a local control-socket handoff. Revisit if this proves annoying in practice — the socket is the natural foundation for a future fleet dashboard anyway.

---

## 10. Open items — not yet designed

These are named explicitly so they don't get silently assumed during implementation.

**Still open *for v1* (must be designed before/within the build):**

- ~~**`NANA.md` content**~~ — **resolved (phase 7), reworked (#61):** the skill doc is now **minimal** (`internal/skill/NANA.md`, embedded) — it defaults the agent to the `popo` client (`popo help`/`popo <verb>`) and the absolute-path contract. The full raw protocol (maildir flow, counter-based heartbeat liveness §4.7, optional capability self-report, and the eight-verb table) moved to `internal/skill/PROTOCOL.md` as the file-I/O-only fallback. Both are dropped at bootstrap; `skill print [--protocol]` surfaces them.
- ~~**Audit log schema**~~ — **resolved (phase 6a, §6 floor):** controller-side JSONL, `{ts, id, type, url, method, venue, status_code, body_size, body_sha256, outcome}`. Phase 6b extends entries with fetch **rewrites** (original + rewritten URL) and **approval** outcomes (auto-allowed / one-shot / persisted / denied), per §6.1.
- ~~**Approval persistence format**~~ — **resolved (phase 6b):** `internal/egress/store.go` persists allow/deny rules to `approvals.json` (`AddAllow`/`AddDeny`), host-scoped by default with custom globs via `--remember`; deny rules checked before allow (#31–#32). Rule expiry isn't implemented (rules persist until removed) — revisit only if demand appears.

**Resolved since last revision:**

- ~~`pending`/`approve`/`deny` trigger~~ — **resolved (§6.1):** approval gates **controller-venue egress** that survives rewriting, not "unlisted domains." `approve --remember` persists a rule.

**Future refinement — generalized platform/target descriptor.** Tier-1 wheel tags (§5) currently derive a *generous* musllinux/manylinux superset from `arch`+libc-*family*. A language-agnostic improvement: capture `Platform{os, arch, libc{family, version}}` in the probe fingerprint (the gap today is the libc **version**) and have each package manager derive its own identifiers from it (pip tags, cargo `<arch>-unknown-linux-<musl|gnu>`, Go `GOOS/GOARCH`, npm `os/cpu/libc`) — optionally overridden by querying the sandbox's own toolchain (`pip debug`, `rustc --print cfg`) when present. The generous superset is correct meanwhile; build this with the second package manager (where the abstraction earns its keep).

**Parked for v2 (§0) — preserved, not designed now:**

- **Sub-agent loop** (`web.research`, Tier 2/3 fallbacks) — native Go against the Messages API with `web_search`; iteration/stopping criteria, prompt construction, partial-progress reporting all undesigned. See §4.5.
- **Tier 2 build environment** — which container/VM strategy mirrors a probed (arch, libc, python version) fingerprint closely enough to produce ABI-compatible wheels. See §5.
- ~~**`ExecFS` bulk-transfer wire protocol**~~ — ✅ **resolved (decision #55):** a runtime tree pushes in one `tar -xf -` exec (no custom protocol), so runtime installs no longer require SFTP.

---

## 11. Decision log

| # | Decision | Rationale |
|---|---|---|
| 1 | No daemon required inside the sandbox | Popo polls via its own SSH session; minimizes assumptions about the sandbox |
| 2 | Absolute-path contract, not PATH/profile edits | Non-interactive exec per tool-call can't reliably source rc files |
| 3 | python-build-standalone (astral-sh) for portable Python | Active, relocatable, maintained; confirmed current as of this doc |
| 4 | Maildir pattern (tmp/new/cur) for outbox/inbox | Atomicity and crash-safety for free, transport-agnostic |
| 5 | Tiered package resolution, mirror-first | Popo can't reach the internal mirror itself; sandbox can |
| 6 | Two fetch venues (controller vs sandbox-exec) | Popo and sandbox sit on opposite sides of a network boundary |
| 7 | ~~`unlisted_domain_policy: allow_and_log`~~ → **controller-venue fetches are gated** (held for approval), with persistent operator allow-rules + a rewrite/redirect table; non-configurable SSRF floor underneath | Controller-venue fetch is a tunnel through the sandbox's egress isolation (exfil risk); gate it, but let approval be made permanent and let requests be redirected to internal mirrors (§6.1) |
| 8 | Heartbeat-only liveness, no `pending` stub; content is **`<seq> <ts>`**, liveness judged on counter advancement | Simpler; counter avoids false-downs from clock skew between Popo and an agent harness with an unreliable clock |
| 9 | `ExecFS` fallback built from day one, not deferred | SFTP subsystem disabled is a real, not hypothetical, failure mode |
| 10 | No `/tmp` fallback for install root; fail and ask operator | Ephemeral installs defeat the point |
| 11 | `install` vs `serve`: lock-and-fail-fast, not a control socket | Simpler for v1; socket revisited only if needed |
| 12 | Tight v1 / v2-backlog split; Tier 2, `web.research`, fleet, ExecFS bulk-transfer parked with re-entry triggers (ExecFS bulk-transfer later un-parked — #55) | "Ship the minimum that solves the problem" — keep the thinking, don't build it speculatively |
| 13 | At-least-once delivery, deduped by `id` to effectively-once; `cur/` swept on `serve` start; non-idempotent (`web.fetch` non-GET) recovered requests fail rather than auto-retry | Transport can drop/double-deliver; replaying a POST is unsafe |
| 14 | `capabilities.json` trimmed to `has_exec` + `has_file_write` *(superseded by #72 — now a controller-written host+agent self-report)* | Drop fields nothing consumes (`has_network_tools`, `shell_hint`); re-add when a consumer exists (§4.6) |
| 15 | Stack: Go 1.26, cobra CLI, SSH host-key verification via known_hosts (no `InsecureIgnoreHostKey`) | Idiomatic Go CLI; secure-by-default transport (washu security floor) |
| 16 | `RemoteFS` is `WriteFile`+`Rename` primitives; atomic delivery composed in the maildir layer (not a `WriteAtomic` FS method) | Keeps `new/` partial-free via `tmp/`→`new/` rename; simpler FS contract proven identical across both transports |
| 17 | Transport auto-selected (SFTP, else Exec) with `--transport` override | Auto for real use; override lets the conformance/E2E suite exercise ExecFS even where SFTP works |
| 18 | `python.install` resolves via PBS `latest-release.json` + `SHA256SUMS`, sha-verified, recording the exact pinned patch | Low-maintenance vs an in-code version table; still deterministic per install (§4.2) |
| 19 | `RemoteFS` gains `Chmod` + `Symlink`; ExecFS palette += `chmod`, `ln -s` | A relocatable interpreter needs an executable bit and symlinks (`bin/python3`→`python3.x`) |
| 20 | python.install push is transport-agnostic (SFTP or ExecFS); shebang rewriting deferred to phase 4 | The abstraction already gives a uniform push; `bin/python3` runs by absolute path, so shebangs only bite once pip installs console scripts |
| 21 | `pip.install` = **resolve (co-resolved, native) → retrieve (per-package)**; requests may be unversioned but the resolution is recorded with exact versions + sha256 | Native resolution fidelity *and* per-package pull attribution; determinism at the resolved layer (supersedes "always-pinned request") |
| 22 | Per-manager verbs (`pip.install`, later `npm.install`…) over manager-neutral types in `internal/pkg`; build one manager at a time | Multi-language by shape without speculative framework (washu simplicity) |
| 23 | Tier 0 points pip via explicit `--index-url` flags; `bootstrap` also writes `state/pip.conf` | Flags are robust under non-interactive exec (§2); pip.conf lets the agent's ad-hoc pip reach the mirror too |
| 24 | Tier 1 = controller cross-platform `pip download` → relay wheels via `RemoteFS` → offline `--no-index --find-links` install; tier=relay, sha256 from the wheels | The sandbox needs nothing; Popo's network does the fetch; the resolve→retrieve shape carries over (retrieval moves to the controller) |
| 25 | Tier selection explicit: `--tier auto\|mirror\|relay` (auto = mirror if `index_url` set, else relay). **`auto` now falls back mirror→relay** when the mirror can't resolve a spec; explicit `--tier mirror` is respected | Predictable, with a safety net: a package on PyPI but absent from the internal mirror still installs via the relay instead of failing. (Implemented post-v1.) |
| 26 | Tier 1 requires `python3`+pip on the **controller** (operator's machine), validated with a clear error | The controller is operator-controlled (unlike the sandbox); a self-contained controller-PBS is deferred |
| 27 | `RemoteFS` gains `RemoveAll` (recursive, idempotent — `rm -rf` / sftp recursive); Tier 1 uses it to clean its transient relayed-wheel dir | A generic delete primitive the contract was missing; closes the wheel-litter gap |
| 28 | `web.fetch` sandbox venue uses **curl, else busybox `wget`** — never Python | web.fetch is a language-agnostic capability; tying it to the Python runtime would be wrong (Python is just our first language) |
| 29 | Split phase 6 → **6a** (sandbox venue + audit, no exfil hole) and **6b** (controller venue + SSRF DNS floor + gating + rewrites + approval) | Land the safe, useful mechanism first; the security-critical policy layer is its own focused phase |
| 30 | Audit log = controller-side append-only JSONL per `sandbox_id`; `{ts,id,type,url,method,venue,status_code,body_size,body_sha256,outcome}` | Popo's copy is authoritative (§3); closes the §10 OPEN item |
| 31 | `approve <id>` grants **host scope** (`https://host/*`); `--remember` for a custom glob; URLs normalized to a path so host rules match bare URLs | One OK unblocks a whole site (practical); normalization avoids a re-submit staying held (bug found in 6b) |
| 32 | Unlisted URL → **controller venue, gated** (held for approval); `deny` persists a deny rule checked before allow | The approval flow exists for exactly this; deny must stick across re-submits |
| 33 | Controller SSRF floor blocks **private** too (Popo is outside the corp net), enforced at **dial** time; literal blocked-IP URLs refused up front for both venues | Rebinding-resistant; the floor sits underneath rewrites/allow rules (§6) |
| 34 | `NANA.md` embedded (`go:embed`) and dropped at bootstrap; `status` reads `capabilities.json` if present | The skill doc ships with the binary (always in sync); the v1 cap-stone surfaces liveness/queue/capabilities |
| 35 | **Acceptance demo:** a real Claude agent inside an **air-gapped** Alpine VM, driven by `NANA.md`, must bridge through Popo for Python/packages/web | Functional tests run with open internet (prove the *mechanism*); only an air-gapped real-agent run proves the **premise** — that a sandbox which can't reach the network is bridged by Popo |
| 36 | Air-gap = default-drop egress, allow only DNS + 443 to Anthropic's published ingress ranges (`160.79.104.0/23`, `2607:6bc0::/48`); INPUT left open so Popo's inbound SSH survives | Stable, documented ranges keep the agent itself reachable while PyPI/GitHub/data-URLs are blocked; the only tunnel out is Popo. IPv6 dropped too, or egress leaks |
| 37 | Demo agent authenticates with a **subscription** token (`CLAUDE_CODE_OAUTH_TOKEN` from `claude setup-token`); `ANTHROPIC_API_KEY` always emptied at launch | Subscription billing, never the metered API; emptying the key makes accidental API billing impossible |
| 38 | Two run modes: `make demo` (headless, pre-approve the fetch host, assert the artifact — CI gate) and `make demo-live` (operator approves the egress hold live) | A one-shot headless `claude -p` can't wait for a mid-run approval — it surfaces the `approve` cmd and exits (itself a gate demo); unattended runs pre-approve, live runs keep the human in the loop. Re-submit after approval needs a **new id** (maildir dedup, #13) |
| 39 | Demo runs on **Alpine/musl** like the functional box (Claude Code needs `libgcc`/`libstdc++`/system `ripgrep` + `USE_BUILTIN_RIPGREP=0` + `bash`); a glibc/Ubuntu variant is parked | Same musl realism end-to-end with a real agent; we can't assume the sandbox distro, so glibc validation is a later phase |
| 40 | **Activity log + `logs` two-tail.** `serve` records a per-sandbox JSONL activity stream (`internal/activity`, one event per serviced request + operator approve/deny) via a decoupled `Dispatcher.Observe` callback; `iceclimber logs -f [--agent-log]` merges it (`[POPO]`) with the agent's tee'd stream (`[NANA]`). JSONL is the source of truth; a graphical TUI is parked v2 (§0) | An opaque `serve` made runs hard to follow; structured JSONL keeps `protocol` decoupled from logging and seeds the future TUI, while the merged viewer satisfies "two tails side by side" now without new deps |
| 41 | **Supervised `serve`.** On a TTY (not `--once`/`--yes`), prompt the operator Claude-Code-style with context before **every** operation and approve inline: a `Dispatcher.SetGate` pre-execution hook covers installs (deny → `operator_denied`); `web.fetch` self-gates in its handler. Inline approval **proceeds to the real fetch in the same pass** — dissolving the two-pass `needs_clarification`/re-submit (#38, #32). `--yes`/non-TTY = unattended (installs auto-run, fetches → pending); `--supervise` forces prompting without a TTY (scriptable; reads stdin — also how the functional test drives it) | Out-of-band pending/approve was clunky and forced re-submits; a synchronous prompt is the natural human-in-the-loop, and blocking the dispatcher is fine for one operator/one sandbox. Decoupled via callbacks so `protocol`/`webfetch` stay UI-agnostic |
| 42 | Liveness during a blocking prompt: the approver refreshes the heartbeat (`WriteHeartbeat`) **right before** reading input — same goroutine, no fs race | The agent judges Popo-alive on `seq` advancing (#8); a stalled loop during a long prompt would read as "down". Keepalive keeps it honest without a concurrent heartbeat goroutine (revisit if a single decision can exceed ~2 min) |
| 43 | Demo program **computes** a report (rich + pyfiglet: ASCII banner + stats table + bar chart) from **one** fetch; verify asserts both imports + the recomputed `len(title)` | Proves a real program ran (not an agent echo) and exercises two libraries, while a single fetch keeps token cost low |
| 44 | **Node/npm — the second language.** `internal/node` (mirror `internal/python`) installs a portable Node — glibc from nodejs.org/dist, musl from the unofficial-builds mirror; both ship `.tar.gz`, so the Go gzip stream-push carries over with no xz dependency. `internal/npm` (mirror `internal/pip`) installs **globally into the runtime prefix** and returns a **`NODE_PATH`** the agent exports: **Tier 0** runs the sandbox's npm against a registry; **Tier 1 relay** has the controller's npm stage the install and Popo relays the `node_modules` tree in (sandbox runs no npm) | Realizes the multi-language design (#22) and roughly doubles the addressable agents; the neutral `internal/pkg` types + RemoteFS push carried over unchanged. **Pure-JS only** — native addons get the controller's platform and are the Node "Tier 2" (deferred, like pip's sdist). musl **arm64 needs Node ≥ 24** (older musl builds are x64-only) |
| 45 | **Observability TUI** (`iceclimber tui`) — a Bubble Tea split-pane `[POPO]`/`[NANA]` dashboard over the activity JSONL; `--snapshot` renders one frame headlessly (testable/scriptable). The file-tailing is shared with `logs` via `internal/tail` | The two-tail logs proved useful (the re-entry trigger), and the JSONL was already the data source. First TUI framework dep (bubbletea + lipgloss), justified by the feature. (A live SSH-polled status view was later delivered in the console — #53's `s` panel.) |
| 46 | **TUI-first operator console** (bare `iceclimber`) — the TUI **embeds serve** (one process): runs the dispatcher in-process, streams **live** activity to the panes (via `Dispatcher.Observe`, not JSONL tailing), and surfaces every approval as an **inline modal** answered in-place. Built on an `asker` seam in the approver — `terminalAsker` (stdin) vs `tuiAsker` (pushes an `ApprovalRequest{…, Reply chan}` to the console's event channel and blocks); the shared gate/`ApproveFetch`/remember/log routing is unchanged, so the dispatcher gate (installs) and `web.fetch` (egress) both drive the modal and still return the real result in **one pass**. Quit cancels the serve ctx; a pending approval fails safe to **deny** via the done channel. Subcommands stay for scripting/CI; **`iceclimber serve` is the headless path** and **`iceclimber tui` is the passive attach view** (both share the `internal/tui` renderer). Architected per-`sandbox_id` for a future fleet | "Any sort of acceptance should be done from within the TUI" — a single cockpit beats juggling `serve` + `logs` + out-of-band `approve`. Embedding serve (vs attaching) is what makes inline approval possible; the earlier `Observe`/`SetGate`/`Approver` seams (#40–#41) made it a wiring change, not a rewrite. **Phase 1** of an incremental console: serve + watch + approve now; **Phase 2** = management actions in-TUI (trigger installs, egress rules + pending approve/deny/forget, bootstrap/re-provision, fuller status polling); **Phase 3** = multi-sandbox list/switcher + serving several at once |
| 47 | **Operator-initiated installs + bootstrap from the console** (Phase 2a of #46). `i` opens an install form, `b` a bootstrap confirm; the operator drives the sandbox from within the TUI, not only observes/approves. **The form presents two languages — Python and JavaScript — and the packages to install; the runtime is auto-installed as needed (a bare runtime is a CLI concern, judged an unlikely TUI need), the package manager (pip / npm) and tier (auto) are *derived*, and a blank version takes a recommended default (Python 3.12 · JS/Node 24).** huh binds to a **pointer-shared `formState`**, not `&c.field` — the value-receiver Console is copied by Bubble Tea, so binding to a field address dangled to a stale copy and the operator's selections never reached `submitForm` (every install came out Python). Validated two ways: model-level tests **drive the real huh form via key events** (selecting JavaScript must reach the request — the binding regression), and a **functional test runs the executor against the live sandbox** (`make tui-functional`: Python/JS install + bootstrap, asserting the Nana echo). Built on an injected **`OpRunner`** seam (mirrors the asker seam): the `tui` owns the form UI (**charmbracelet/huh**) + a neutral `InstallRequest{Lang,Action,Version,Pkgs}`; the cli's `consoleOps` holds the session, maps language+action → installer (`python.install`/`pip.install`/`node.install`/`npm.install`), and executes off the UI goroutine, emitting a `KindOperated` activity event (so the `[POPO]` pane + JSONL reflect it like a serviced request) + an `OpResultMsg` to clear the running indicator. The running state single-flights operator actions; the approval modal still preempts. Bootstrap reuses an extracted `provision()` (tree + pip.conf + NANA.md + ping/pong smoke test); destructive `--force` stays CLI-only. huh hides at the group level, so the packages field lives in a second group skipped for a runtime install | The console promised "manage the sandbox from within"; observe+approve (#46) was only half. **The operator thinks in languages (Python/JS), not package managers — pip/npm/tier are implementation details to derive, and sane default versions mean install is one keystroke.** Pre-provisioning without waiting for the agent to ask is the natural next capability, and the `OpRunner` seam keeps `tui` free of session/install knowledge just as the asker seam kept it free of approval-routing. First form dependency (huh), justified by Phase 2's growing form surface (egress rules next) |
| 48 | **Sandbox-side verification echo ("Nana's voice").** Operator actions confirm themselves *in the sandbox* and surface the result in the `[NANA]` pane: a runtime install runs the interpreter (`python -V` / `node --version`) over `sess.runner` and echoes its banner; a package install confirms presence via `pip show` (dist name) and a `node_modules/<pkg>/package.json` read (dir name) — both keyed on the **install name**, not the import/require name; bootstrap reuses its ping/pong smoke test (already a sandbox round-trip). Carried on a new `activity.Event.Side` (`popo`/`nana`) + `KindVerified`; the console routes `nana`-side events to the `[NANA]` pane, which it now **always** shows (useful even with no `--agent-log`/agent) | The console showed Popo *doing* installs while the sandbox stayed silent — "nothing happens on Nana even though the action is on Nana." Output captured from running *in* the sandbox is independent confirmation the artifact works there, not just that Popo's push returned. **Presence checks (not import/require) sidestep the name-mismatch false negative** (`Pillow`→`PIL`, npm require-names). Reframes the panes — POPO = what the controller did, NANA = what the sandbox reports — and generalizes to agent-serviced installs later |
| 49 | **All TUI flows tested through the real runtime (`teatest`), as a rule.** `internal/tui/flows_test.go` drives the console with the genuine Bubble Tea event loop and huh form — every key, modal (egress/operation approve/deny, preempt-quit), form field (language select, optional/runtime-only packages, version-default, abort), bootstrap confirm/decline, running indicator, live activity routing (POPO vs NANA), and quit. Assertions combine rendered output (`WaitFor`) with recorded operator actions (channels) and the `FinalModel` state. Hand-rolled cmd-pump approximation removed. The console executor also has a white-box functional test against the live VM (`make tui-functional`) | The TUI is now a primary surface; "approximating the runtime" (the earlier pump) isn't definitive. teatest runs the *real* loop, so a flow that passes is genuinely correct — the same confidence the CLI had from stdin/stdout tests. Gotchas encoded: teatest drains its output reader across `WaitFor` calls (so same-frame substrings check together) and the narrow POPO pane truncates (assert long detail via `FinalModel`); huh's async field advance needs a per-field focus checkpoint before typing |
| 50 | **Headless parity + full-stack TUI test.** Bare `iceclimber` launches the console only on a **real terminal**; with no TTY (CI, pipes) it falls back to the unattended `serve` loop (`runHeadless`), so the entire command-line surface keeps working with the TUI present — every console action has a subcommand equivalent. Validated end to end: a black-box test runs the bare binary in a non-tty and asserts the headless banner (`test/functional/headless_test.go`), and `TestConsoleTUI_FullInstall` runs the **real console program** (teatest) wired to the **live sandbox**, drives the install form by keystroke, and asserts the package actually lands in the sandbox *and* Nana's `✓ … present` echo renders — the TUI analogue of the app-building suites. **Verification confirms the *requested* package (by name via `pip show` / `node_modules`), not just what was newly installed**, so an already-present package is still confirmed (`already satisfied` + echo) rather than met with silence | A TUI that replaces the CLI entry point must not break headless use; the fallback keeps CI/pipe/scriptable operation intact. And "full tests, like the agent builds an app" means driving the *actual program* against a real sandbox, not just the executor — surfaced the by-name verification gap (idempotent re-install showed "0 installed" with no confirmation) |
| 51 | **Java — the third language (JDK runtime first).** `internal/java` mirrors `internal/node`: a portable **Temurin JDK** resolved via the **Adoptium API** (`/v3/assets/latest/{feature}/hotspot`, the analog of Python's PBS metadata — per-build SHA256, `.tar.gz` so the gzip stream-push carries over with no xz). musl is the `alpine-linux` os, glibc is `linux`; arch maps `x86_64`→`x64`/`aarch64`→`aarch64`. The tarball's top dir is auto-stripped from the first entry (no dependency on the `jdk-21.0.11+10` name). `install java <version>` + the `java.install` verb. **Verified on the aarch64/musl VM:** installs JDK 21, runs `java -version`/`javac`, and compiles+runs a program via single-file launch (JEP 330). **Staged per #22 — runtime first; dependency resolution (Maven coordinates via Coursier) is the next increment, and is *easier* than pip/npm in the relay case because JVM bytecode/JARs are platform-independent (no cross-platform-wheel spike, no native "Tier 2" except JNI).** | Third language realizes the multi-language design; the runtime installer is a clean mirror, and the Adoptium API gave a maintained, checksum-backed musl/aarch64 source (de-risked first, like the Node "musl arm64 ≥ 24" lesson). Runtime-first ships value fast and keeps the bigger dependency-resolution work as its own focused increment |
| 52 | **JVM dependency resolution via Coursier (Tier 0).** `internal/maven` resolves Maven coordinates (`group:artifact:version`) and their transitive deps into a **classpath** — the JVM analogue of `pip.install`/`npm.install`, returning a `classpath` the agent runs with `java -cp` (as npm returns `NODE_PATH`). The resolver is **Coursier**: its 25 kB JAR launcher is fetched on the controller (cached) and relayed into the sandbox once (platform-independent), then run via the installed JDK — `java -jar coursier.jar fetch --classpath <coords>` against Maven Central (or a configured `repository_url` mirror), with `COURSIER_CACHE` under the install root. Coursier resolves the whole set in one unified resolution, so the request succeeds/fails as a unit (vs pip/npm's per-package). **Tier 1 (relay):** the **controller's** java runs the same launcher to resolve + download the JARs on Popo's network (`controller_java`, default "java"; validated with a clear error like pip's #26), then Popo relays the JARs into `<root>/runtimes/maven-relay` and the returned classpath references the relayed copies — **trivially correct because JVM bytecode is platform-independent** (no cross-platform-wheel spike, no native "Tier 2" except JNI). `auto` picks relay when no sandbox-reachable mirror is configured (the air-gap default, like pip/npm #25). `install maven … --java VER [--tier]` + the `maven.install` verb. **Both tiers verified on the aarch64/musl VM:** Tier 0 resolves Guava + transitive deps and compiles+runs a program; Tier 1 relays them in (controller-resolved) and compiles+runs against the relayed classpath. | Realizes the Java package manager (#22). Coursier gives transitive resolution without a build project, and its launcher is platform-independent so the *same* launcher serves both tiers (sandbox JDK for Tier 0, controller java for Tier 1). Staged Tier 0 then Tier 1 (mirror pip phases 4→5); the relay is the air-gap path and is simpler here than for pip/npm because JARs need no per-platform build |
| 53 | **Console Phase 2b — status panel + egress management.** Two read/manage panels in the console (keys `s`/`e`), gated on the operator backend: **`s`** opens a live **status panel** (heartbeat seq + age, queue depth, installed runtimes for *all* languages, agent capabilities) — `collectStatus` is factored out of the `status` command and shared; the panel polls over SSH (async `PollStatus()` → `StatusMsg`), `r` refreshes. **`e`** opens an **egress view** listing held **pending** requests + persisted **allow/deny rules**, with `a`/`d` to approve/deny a pending (host allow/deny + drop, mirroring `iceclimber approve/deny`) and `f` to **forget** a rule (new `Store.RemoveAllow`/`RemoveDeny`); egress ops are controller-local (no SSH), read live via `Egress()`. The console's `OpRunner` seam grew these methods; `consoleOps` implements them over `sess`/`sess.policy.Store()`. teatest flows cover both panels (open, content, navigate, approve/forget, close) | Completes the console's "manage the sandbox from within" promise (#46 Phase 2). Mostly wiring existing capability (`status`, the egress store, `approve`/`deny`) into the TUI behind the same seam, so `tui` stays free of SSH/session knowledge. In supervised console use egress is approved inline (modals), so the view's real value is curating the *accumulated* allow/deny rules; pending is shown for stragglers from headless runs |
| 54 | **Pre-Phase-3 hardening (Phase 1+2 consolidation).** A robustness/coverage/backlog audit before the multi-sandbox addition. Fixed: the egress panel **surfaces action errors** (approve/deny/forget were swallowed, then masked by a stale refresh); the **status panel reports an unreachable sandbox** (a failed root probe → an error line, not "polling…" forever); the background `Serve` loop **emits a visible warning** if it dies unexpectedly (e.g. SSH drop) rather than going silently quiet. Added the missing **Python scenario** (`test/scenarios/python/`, parity with node/java per the "Adding a language" checklist), **error-path unit tests for all six install handlers** (malformed/missing params), and a **`ShellQuote` injection round-trip test** (adversarial inputs must come back verbatim through `sh`). Documented the intentional input precedence (modal ⟫ form/panel) and the console goroutine lifecycle. Backlog audit confirmed §10 open items resolved, v2 properly parked, no dead code/TODOs | A TUI that hides failures erodes operator trust, and parity gaps + untested error paths are cheap to close now and costly to find later — consolidate before a big feature. Deliberately skipped as low-value/counter-to-tight-scope: surfacing best-effort activity-log write failures, deadline-scaling the (reliable) teatest timeouts, and thin cobra-wrapper unit tests already covered functionally |
| 55 | **`ExecFS` bulk-transfer — runtime tree in one exec (closes the v1 SFTP-only constraint).** A runtime push now goes through a shared `remotefs.PushTarGz`, and any `FS` implementing the new `TreePusher` capability (`ExecFS` does) gets the whole extracted tree as **one** `tar -xf - -C <target>` exec with the tar streamed into stdin — instead of a `cat >`/`chmod`/`ln -s` round trip per file (thousands, for an interpreter). Gunzip + strip-leading-component happen **controller-side in Go** (re-taring on the fly), so the sandbox `tar` needs no `-z`/`--strip-components`: BusyBox `tar` suffices and preserves the executable bit + symlinks. A plain `FS` still falls back to the per-file path. This **removed the v1 "requires `SFTPFS` for `python.install`" constraint** (§6) — all three languages install over either transport. The per-file push (formerly duplicated in each installer's `pushTarGz`, ~270 lines) was unified into `remotefs/pushtar.go`; each installer is now a thin `extractAndPush`. Verified end to end on the BusyBox VM by `Test{Python,Node,Java}InstallOverExec` (install each runtime over `--transport exec`, artifact runs; the Python/Node cases also assert a symlink survived the bulk push, and the JDK case proves a ~200 MB tree streams in one exec) plus unit tests for strip/per-file/bulk paths | The originally-sketched custom length-prefixed wire protocol (§6) was unnecessary: `tar` over one stdin-streamed exec is the same single-round-trip win with **zero new protocol** and no new sandbox dependency (BusyBox already has `tar`). The narrow re-entry trigger (SFTP disabled *and* needs a runtime) was worth closing now while the push code was being unified anyway, and it makes the transports truly interchangeable rather than runtime-push being SFTP-only |
| 56 | **In-CLI host-key trust (`iceclimber trust`) — record the key without leaving the workflow.** First contact with a sandbox needs its SSH host key in `known_hosts`; previously that was an out-of-band `ssh-keyscan`/`ssh`, which is friction — and for **ephemeral** sandboxes (fresh key every spin-up, addresses reused) it's the dominant cost. `trust` fetches the offered key in-process (`remote.FetchHostKey` dials far enough for the host-key callback to capture the key, then aborts before auth — no credentials needed), prints its SHA256 fingerprint, and records it (`remote.RecordHostKey`, creating the file/dir; `--replace` swaps a rotated key, preserving other hosts). **The security floor is never lowered silently:** on a TTY the operator confirms the fingerprint; unattended runs must pass `--fingerprint SHA256:…` (record only on a match — the safe automation path) or `--yes` (trust the current network). Connect failures now surface a typed `remote.HostKeyError` (unknown vs. changed key) whose message names `iceclimber trust`, and the **bare-`iceclimber` console offers a first-connect trust modal** (`tui.TrustPrompt`) then reconnects. Verified: unit tests for `CheckHostKey`/`RecordHostKey`/replace-preserves-others + the typed error, teatest for the modal (accept/decline/mismatch-warning), and functional tests on the VM (`trust` from a clean slate → probe connects; `--fingerprint` mismatch refuses to write) | Host-key recording living *outside* the tool is exactly the kind of out-of-band step that breaks an otherwise self-contained workflow, and ephemeral sandboxes make it recurring rather than one-time. Bringing it in-CLI (and into the console) closes that gap **without** weakening the secure-by-default transport (#15): no `InsecureIgnoreHostKey`, no silent TOFU — every record is an explicit, fingerprint-visible act, and automation verifies against a value captured at provision time |
| 57 | **`iceclimber agent install [claude]` — relay a coding agent into the sandbox.** Promotes the demo-only "install Claude + a token into the VM" glue (was `npm install -g @anthropic-ai/claude-code` baked into `test/lima/demo.yaml`'s first boot + token handling duplicated across `demo-agent.sh`/`demo-run.sh`) to an official command. `internal/agent` is an extensible `Descriptor` registry (Claude first; a new agent is just another entry — npm-prefix, platform-suffix mapping, binary path, token/env). **The install is a pure relay, honouring the air-gap model:** `@anthropic-ai/claude-code` v2 ships a **self-contained per-platform native binary** in a platform package (`@anthropic-ai/claude-code-linux-arm64-musl` is just `package/claude` — no JS, no Node), so the controller resolves that package **for the sandbox's fingerprint**, downloads the tarball on its own network (sha512 integrity-verified), and relays the binary in via the shared `remotefs.PushTarGz` (bulk tar on ExecFS; the `package/` root is stripped so the binary lands at `<root>/agent/<name>/<bin>` with its exec bit preserved). **The sandbox never touches the registry and needs no Node** — it works on a fully air-gapped box. Then a **0600** auth env file is written and the binary is run to verify. **Auth is subscription-only and never logged:** the token comes from the descriptor's env var (e.g. `CLAUDE_CODE_OAUTH_TOKEN`) or `--token-file`, an API key (`sk-ant-…`) is rejected, the env file blanks `ANTHROPIC_API_KEY` (no fall-back to metered billing), and the token reaches the sandbox only as FS-write content, never as a command argument. The demo was rewired onto the command: `demo.yaml` drops node/npm/claude (keeps only the musl prereqs iceclimber doesn't give — ripgrep/bash/libstdc++/…), `demo-run.sh`/`demo-live.sh` run `agent install claude` after bootstrap, and `demo-agent.sh` launches via the `nana` launcher (#58). Verified: unit tests (descriptor lookup, platform→package mapping, API-key rejection, token-file parsing, env-file rendering/quoting) + a functional test that relays Claude onto the musl VM and runs `claude --version`, asserting the 0600 env file + blanked API key | The demo proved the capability; an official command makes it a product feature and kills the duplicated install/token glue (one path, not three). **The relay was non-negotiable: iceclimber exists for sandboxes we don't control and can't open to the network, so an on-target npm install (the obvious first cut) was the wrong model — the controller must do the fetching.** v2's self-contained per-platform binary made the relay *simpler* than the runtime installers (no Node, no postinstall — just fetch the right platform package and push the binary), reusing the bulk-transfer (#55) wholesale. The `Descriptor` seam means a second agent is data, not new machinery; the security posture (subscription-only, API key refused, token never logged, API-key var blanked) carries over from the demo's hard rules |
| 58 | **`nana` launcher — one in-sandbox command to start the agent.** `agent install` also drops `$ICECLIMBER_HOME/nana` (+ a per-agent `$ICECLIMBER_HOME/agent/<name>/run`). The operator runs `$ICECLIMBER_HOME/nana [agent] [-- args]` from any directory: a generic dispatcher selects the agent (the sole installed one, or an explicit first arg naming one; ambiguous → error), strips an optional `--`, and execs that agent's `run`, which sources the env (token, `ANTHROPIC_API_KEY` blanked) and launches the harness with **NANA.md as persistent system context** (Claude: `--append-system-prompt "$(cat NANA.md)"`) plus any passthrough flags. The agent-specific recipe (binary, system-prompt flag) is baked from the `Descriptor` at install time, so the sandbox scripts stay generic and a second agent needs no new launcher logic. **The bug it kills:** the hand-rolled launch (`. env.sh && claude …`) was fiddly and easy to get wrong — operators hit "claude prompts for login" when they forgot to source `env.sh`, or used the wrong `$ICECLIMBER_HOME`. The demo dogfoods it (`demo-agent.sh` → `$ICECLIMBER_HOME/nana -p "…" <flags>`), so a broken launcher fails the acceptance run | A self-contained sandbox should ship the command to *use* it, not just install it; collapsing the env/auth/NANA-wiring dance into one launcher removes a class of operator error (and the demo proving it means the launcher can't silently rot). Chosen after weighing four axes with the operator: path-qualified `$ICECLIMBER_HOME/nana` (no PATH magic), sole-agent default, launch-only (no preflight), and NANA.md as persistent system context (not a one-shot opening prompt) |
| 59 | **`web.fetch` blob path — write where the spec says (`protocol/blobs`).** An agent following `NANA.md` looked for a large response body at `$ICECLIMBER_HOME/protocol/blobs/<sha>` (where the canonical `protocol.Tree.Blobs()` and `bootstrap` put it) but `web.fetch` had written it to `$ICECLIMBER_HOME/blobs/<sha>` via a hardcoded `path.Join(root, "blobs")` that bypassed the accessor. **NANA.md was right; the implementation was the bug.** Both venues (`fetch.go`, `controller.go`) now write through `protocol.Tree{root}.Blobs()`, and the published `body_blob` is a `$ICECLIMBER_HOME`-relative path from the new `Tree.BlobRef(name)` — derived from `Blobs()`, so the reference can never drift from the write location. NANA.md's `body_blob` example is now the unambiguous `protocol/blobs/<sha256>` ("read it at `$ICECLIMBER_HOME/<body_blob>`"). Guarded by three tests: `protocol` pins `Blobs()`/`BlobRef()` and the `$ICECLIMBER_HOME/BlobRef == Blobs()/name` invariant; a `webfetch` unit test drives `Fetch` and asserts the blob lands under `Blobs()` (not `$ICECLIMBER_HOME/blobs`) with `$ICECLIMBER_HOME/body_blob` resolving to it; the functional `TestWebFetch` now asserts the blob is under `protocol/blobs/` on the real VM | The functional test had *masked* the bug: it read the blob via `path.Join(root, body_blob)`, which matched the buggy write location (both wrong at the root) and never checked against the documented `protocol/blobs/`. The fix ties the writer, the published reference, and the spec to a single canonical accessor (`Blobs()`/`BlobRef()`), and the new invariant test makes a future divergence fail loudly. (Related, deferred: `pip` relay stages wheels in a transient `$ICECLIMBER_HOME/blobs/wheels-*` — same stray name, but internal and cleaned up; rename pending) |
| 60 | **The agent's stream reaches every view with no `--agent-log` flag (the bridge).** The `[NANA]` pane was fed only by a controller-local `--agent-log` file (the demo tee'd `limactl shell … | tee host-file`), so an operator who just ran the agent saw an empty pane while requests scrolled by in `[POPO]`. The fix is a bridge in three parts: (1) run headless, the `nana` launcher mirrors the agent's stdout+stderr to `$ICECLIMBER_HOME/agent/<name>/session.log` — gated on "stdout isn't a tty **or** a print flag (`-p`/`--print`) is present", so a true interactive run keeps its tty (a pipe would break the TUI) while a headless `-p` run *from a terminal* is still captured; (2) the **serving process** (the embedded-serve console *and* headless `serve` — whichever holds the SSH session) runs `bridgeAgentLog`/`pollAgentLogs` to copy new lines from each sandbox `agent/*/session.log` (offset-tracked; name-prefixed when several agents) into a controller-side `agentLogPath` (`~/.iceclimber/<id>/agent.log`); (3) the console, `iceclimber tui`, and `iceclimber logs` all **default `--agent-log` to that file**, so `[NANA]` shows the agent with no flag in any view. The empty-pane hint is now actionable ("run `$ICECLIMBER_HOME/nana` in the sandbox"); `--agent-log` stays an explicit override. The demo dogfoods the whole path (`demo-agent.sh` → `$ICECLIMBER_HOME/nana -p`, no host-tee; `make demo-logs/tui/console` pass no flag). Dogfooded by tests: functional — a headless `nana` writes `session.log`, and a background `serve` bridges a sandbox `session.log` to the controller `agent.log`; cli unit — `pollAgentLogs` over a real ExecFS (single/incremental/truncation/multi-agent); teatest — the hint + a tailed `agent.log` render in `[NANA]`. **Stream rendering:** run the agent with `--output-format stream-json --verbose` and the bridge (`formatAgentLine`) renders Claude's per-turn JSON events into readable pane lines — the assistant's narration and `→ Bash: <cmd>` tool calls (init/tool-result noise dropped); non-JSON passes through. The demo adds the flag, so `[NANA]` shows the agent's live tool calls, not just its final summary (a functional test asserts a stream-json event bridges through as a formatted line) | The console promised a single pane of glass, but the agent runs *in the sandbox* and its stream lived nowhere the controller could see without manual tee'ing. Capturing at the launcher + bridging through whichever process already holds the session lights up *all* views (console/tui/logs) with zero setup — not just the console. The capture gate is the crux: always-tee breaks interactive Claude's UI, but pure tty-gating misses a headless `-p` run launched from a terminal (the live demo's case), so it keys on "headless = not-a-tty **or** print-flag". Routing through one controller-side file (rather than each view opening its own SSH tail) keeps the passive `tui`/`logs` offline-capable and avoids N concurrent sandbox connections. Architected per-agent so multi-agent — and later multi-sandbox — fan-out slots in |
| 61 | **`popo` client + minimal NANA.md — move protocol ceremony off the agent.** NANA.md was ~215 lines and made the agent *hand-execute* the protocol every run: build the JSON envelope, the tmp→new atomic rename, poll the inbox, track the heartbeat seq with backoff, parse responses. That's token-expensive and non-deterministic (a wrong id, a forgotten rename, a misparse silently break it). Now a tiny **`cmd/popo`** client runs *in* the sandbox and owns all of it: `popo <verb> …` builds+delivers+polls (heartbeat-aware)+parses and prints a clean result (exit 0/1/2; 2 = relay-for-approval). It does only local file I/O and reuses the new leaf **`internal/wire`** package (envelope, `Tree`, `NewID`) so its wire format is literally the controller's — no drift; `protocol` re-exports `wire` via type aliases so no caller changed. The client is cross-compiled `CGO_ENABLED=0` (one static binary per GOARCH runs on musl+glibc), `go:embed`'d (`internal/popobin`, built by `make`), and **bootstrap relays it to `$ICECLIMBER_HOME/popo`**; `nana`'s env puts `$ICECLIMBER_HOME` on PATH + sets `ICECLIMBER_HOME`, so the agent just runs `popo` (no `$ICECLIMBER_HOME` knowledge, no JSON). **NANA.md shrank to ~30 lines** (model + "run `popo help`, then `popo <verb>`" + run-by-absolute-path + the exit-2 approval rule), and since `nana` injects it as the system prompt (#58) it survives compaction. **File-I/O-only is preserved as a first-class fallback** (per the air-gap ethos): the full raw protocol moved to **`PROTOCOL.md`** (also dropped at bootstrap, *not* in the system prompt), and NANA.md points there for harnesses that can't execute — the agent self-selects on its exec capability. Dogfooded: `cmd/popo` unit tests (arg→params per verb, result formatting, a local stub-Popo round-trip), a functional test that bootstrap drops `popo` and `popo ping`/`popo python.install 3.12` round-trip on the musl VM against a live `serve`, and skill tests asserting NANA.md stays minimal (and doesn't leak raw mechanics) while PROTOCOL.md keeps the full contract | The agent shouldn't re-derive deterministic plumbing on every request — that's the canonical "make it a script" case, and it both burns tokens and invites silent protocol errors. A compiled client (vs sh) was required because robust JSON can't be parsed in pure busybox sh and the sandbox has no runtime at bootstrap; CGO-off Go gives one portable static binary with full UX control and no external dependency, and reusing `internal/wire` makes the client and dispatcher provably speak the same format. Keeping the file protocol as a documented fallback honours the original "file-I/O-only" design value for harnesses that can't (or won't) exec — the operator trades a tiny exec requirement for far fewer tokens and deterministic behaviour, or keeps pure files if they prefer |
| 62 | **TUI install form: packages are optional (runtime-only install).** The console install form required at least one package (`required("at least one package")` on the packages field), forcing the operator to name a package just to install a runtime — illogical and inconsistent with the CLI, which never required one. **The agent, not the operator, decides what to install** while it writes code; an operator install is usually just the bare runtime. The validator is removed: the packages field is now optional (labelled "packages (optional)" with a "blank installs just the runtime" hint), and `consoleOps.doInstall` short-circuits each language to a runtime-only result (`ensure{Python,Node,Java}` then return) when no specs are given — never calling pip/npm/maven with an empty spec list. The teatest `TestFlow_InstallRequiresPackages` (asserted a validation error blocked submit) became `TestFlow_InstallRuntimeOnly` (asserts blank packages submits with empty `Pkgs`) | The form encoded a "packages-first" stance (#47) that put the operator in the agent's seat — but the operator pre-provisions a runtime; the agent installs packages as its code needs them. Requiring a package made the common case (just give me Python) impossible without inventing one, and diverged from the CLI's behaviour for no reason. Short-circuiting on empty specs (rather than passing `[]` through) keeps the runtime-only path explicit and avoids relying on each package manager's empty-list semantics |
| 63 | **`nana` auto-injects the stream format on a print run — rich `[NANA]` with no flags.** Decision #60 lit up `[NANA]` from the bridged `session.log`, but seeing the agent's **tool calls** (not just its final answer) still required the operator to remember `--output-format stream-json --verbose` on every `nana -p` run; forget them and the pane showed only the summary. The `run` script now injects the descriptor's new `StreamArgs` (`--output-format stream-json --verbose`) automatically. **Gated on the print flag (`-p`/`--print`), not bare headlessness:** a `-p` run is a non-interactive task whose stream a human isn't reading directly, and `-p` is exactly what makes `stream-json` valid — so injection fires there, while `nana --version` and other no-tty diagnostics stay clean (the earlier "gate on headless" draft would have polluted `--version` and broken its functional test). A caller's own `--output-format` always wins (`have_fmt` scan, incl. `--output-format=value`), and interactive runs are untouched. `StreamArgs` is a `Descriptor` field (empty ⇒ no injection), so it stays per-agent. The demo now passes **no** stream flags (`demo-agent.sh` → `nana -p` only), dogfooding the injection end-to-end; verified by `sh -n` + behavioural traces (`-p` injects, `--version`/`-p --output-format text`/non-`-p` don't) and `TestRenderRun`/`TestRenderRun_NoStreamArgs` | The single-pane-of-glass promise shouldn't depend on the operator memorising three flags every launch — the common case (`nana -p "task"`, watch the console) should be rich by default. Gating on `-p` rather than the broader "headless" (`!tty || -p`) is the crux: `stream-json` requires print mode, and bare headlessness also covers `--version`/`--help` where injected flags would corrupt the output, so keying on the print flag is both safer and the precise signal of "this is a task whose narration belongs in `[NANA]`." Keeping it a `Descriptor` field (not hardcoded) means a second agent opts in with data, like the rest of the agent machinery (#57) |
| 64 | **`make release` — cross-built distributables; popo embed is host-independent.** A `release` target cross-builds `iceclimber` for `darwin/{amd64,arm64}` + `linux/{amd64,arm64}` (`CGO_ENABLED=0`, version stamped via `-ldflags -X …cli.version`) and packages each — plus the sandbox-side `popo` client for `linux/{amd64,arm64}` — into `dist/` tarballs with a `SHA256SUMS`. A separate `gh-release` publishes `dist/*` via `gh release create` (outward-facing → never folded into `release`). **The key correctness point it documents:** `release` depends on `popo-bins`, which builds *both* linux popo clients regardless of host arch, and `iceclimber` `go:embed`s both — so popo selection is keyed to the **sandbox's** fingerprint at bootstrap (`popobin.Binary(os, arch)`), never the controller's. A macOS/arm64 `iceclimber` therefore bootstraps a linux/amd64 *or* /arm64 sandbox identically; cross-arch host↔sandbox is the normal case, not an edge. (Sandboxes outside linux/{amd64,arm64} get no embedded popo → bootstrap warns and the agent uses the `PROTOCOL.md` file-I/O fallback (#61).) Verified by running `make release`: the native build reports the stamped version, the popo artifact is a static aarch64 ELF, and all checksums verify | Distributing prebuilt binaries makes the tool usable without a Go toolchain, and a make target keeps the build logic in one place (a CI workflow, if added later, just calls it). Splitting `gh-release` from `release` keeps "produce artifacts" (safe, offline) apart from "publish to GitHub" (a deliberate, networked act). The popo-embed note is worth pinning because the obvious worry — "does iceclimber's host arch have to match the VM?" — is a non-issue by construction, and the target's `popo-bins` dependency is what guarantees it stays that way (a plain `go build` would embed nothing) |
| 65 | **Corporate SSH — delegate to the system `ssh` (ssh_config + jumpbox + password).** iceclimber dialed only a direct TCP connection with key/agent auth, so it couldn't reach corporate sandboxes that need `~/.ssh/config` resolution, a **jumpbox**, or **password/keyboard-interactive** auth. Rather than reimplement OpenSSH config semantics, we **delegate to the operator's `ssh` client** and keep our persistent `x/crypto/ssh` client only for the *target* session/SFTP. `remote.buildDialPlan` resolves a host (which may be a Host **alias**) via `ssh -G` — authoritative for `Match`/`Include`/token expansion — with explicit config fields winning; a resolved `ProxyJump`/`ProxyCommand` is reached by spawning `ssh [-F cfg] [-J …] -W host:port jump` as a subprocess whose stdio is wrapped as a `net.Conn` (`remote.proxyConn`) that the target handshake runs over (multi-hop + bastion 2FA handled by ssh; its prompts go to `/dev/tty`, never the data channel). Both `Dial` and `FetchHostKey` route through the plan, so `trust`/the console first-connect modal work **behind a jump** and key host-key trust on the resolved HostName (`remote.ResolveTarget`). Opt-in `password_auth`/`keyboard_interactive` add lazy callbacks (tried only after key/agent fail) that prompt **no-echo via `/dev/tty`** — so headless-serve still authenticates as long as a controlling terminal exists; only a truly non-interactive env (no tty, no agent/key) fails, with an actionable message, and secrets are never logged/persisted. Config gains `ssh.{use_ssh_config,ssh_config_file,password_auth,keyboard_interactive}`; `ssh.user` is now optional and `ssh.port` 0 means "let ssh_config decide" (dial layer defaults 22 last); all four CLI dial sites funnel through one `dialConfig` helper. Missing `ssh` client → silent fall back to a literal direct dial (today's behavior). **Secure-by-default preserved:** strict `known_hosts`, no `InsecureIgnoreHostKey`. Verified live on Lima: a hermetic **self-jump** ProxyJump test (`trust` + `probe` through the jump) and an **automated PTY** password test (probe authenticates via a no-echo prompt), plus unit tests for `ssh -G` parsing, `proxyArgv`, the subprocess `net.Conn`, and auth assembly | Corporate `~/.ssh/config` (Match/Include/canonicalization/`%h%p%r`, multi-hop jumps, 2FA) is exactly where a hand-rolled Go parser drifts from real `ssh` — and the operator has *already* configured `ssh` to reach these hosts, so `ssh -G` + a ProxyCommand subprocess is both less code and correct by construction, while x/crypto still owns the one thing it must (a multiplexed connection with the SFTP subsystem). Gating password prompts on `/dev/tty` (not stdin/a TTY check) was the key call: it keeps headless operation first-class — an unattended `serve` can still prompt for a password — instead of making the feature TUI-only. The live self-jump + PTY tests surfaced two real bugs before merge (a portless `RemoteAddr` that crashed the knownhosts callback; `-F` not propagated to the jump subprocess), exactly the kind the unit layer can't catch |
| 66 | **Live install progress (meter + ETA + transfer mode).** Runtime/package installs move tens of MB over SSH and could take minutes with **no feedback** — the console showed a frozen `⏳ running …` and the CLI blocked silently. A new leaf package `internal/progress` (Event{Phase,Cur,Total,Unit}, a nil-safe `Func`, a **throttled counting `Reader`** ~100ms, and `HumanBytes`/`ETA`) is the one seam: the runtime installers wrap the **download** body (HTTP `Content-Length`) and the **push** reader (the cached tarball's `os.Stat` compressed size — consumed identically by the exec bulk-tar and SFTP per-file paths, so `PushTarGz` is untouched) and emit resolve/verify phase markers; pip reports per-package `Items` (mirror + relay), npm/maven report resolving→installing phases (their batch model). The operator layer adds the transport label (authoritative `session.transport`) since the installer stays transport-agnostic. **Console:** a bubbles spinner + a hand-rendered Unicode bar in the footer — phase + %/bytes/ETA (transfers) or `(i/n)` (packages) + `· via exec`/`sftp` — fed by `ProgressMsg` pushed (non-blocking) on the existing events channel; the spinner ticks only while in flight. **CLI:** a single `\r`-updating line on a TTY, plain phase lines when piped (no escape codes). The agent/dispatcher path passes a **nil** Func (no render surface). Threaded through one `progress.Func` param on the install constructors (`registry.go`); `bubbles` promoted to a direct dep. Verified: progress.Reader throttle/ETA/format units, runtime extractAndPush emits a transfer event reaching the full size over a real local ExecFS, pip per-package events, the CLI printer (TTY + piped), a teatest meter (byte %/transport + `(i/n)`), and a **live VM** test that a fresh-root runtime install pushes a `transferring` ProgressMsg tagged with `sess.transport` | Long, silent transfers read as a hang; the operator needs to know it's working, how far along, and over which path. One throttled-reader seam captures byte progress for *both* transports without touching the push code, and tagging the transport at the operator layer keeps the installer ignorant of it. Per-package vs phase reporting is honest to each manager's model (pip installs package-by-package; npm/maven batch). Pushing progress through the same non-blocking events channel the console already drains means no new concurrency machinery, and dropping a sample under load is harmless (the next supersedes it). Keeping the meter text plain (percent/`(i/n)`/`via …` around the styled spinner/bar) keeps it teatest-assertable |
| 73 | **Observability Phase 2 — maildir GC: a real "unread" count (closes the last symptom #6).** `status` showed a meaningless "N unread" because `QueueIn` counted **every response ever delivered** — nothing pruned the maildir (`outbox/cur` and `inbox/new` leaked forever; the agent read responses but never removed them). Close the lifecycle. **(1) Collect (popo owns it).** Collecting = moving `inbox/new/<id>.json` → `inbox/cur/<id>.json`; afterward `inbox/new` = uncollected (the real count) and `inbox/cur` = collected. **popo performs the move** — automatically in its request/await flow (zero agent effort) and via a first-class **`popo collect <id>`** verb; the only manual move is the documented raw fallback for agents that genuinely can't exec popo (PROTOCOL.md). No new done-marker and **no rework of dedup**: `responseExists` already checks `inbox/cur`, so a collected response still dedups. **(2) Controller GC** (`Dispatcher.gc`, run first each `RunOnce`/`Serve` cycle): a *collected reap* deletes the coupled pair `outbox/cur/<id>` + `inbox/cur/<id>`; a *retention reap* (config `maildir_retention`, **default 1h**, 0 = off) deletes an abandoned pair `inbox/new/<id>` + `outbox/cur/<id>` once the response has sat uncollected past the window — **measured from `CompletedAt` (delivery time), not request creation**, so a long install is never reaped out from under the agent. `serviceCur` stamps `CompletedAt` if a raw handler left it zero. GC is best-effort (a listing hiccup is swallowed — the following `drainNew` surfaces a real drop — and `RemoveAll` is idempotent) and runs **before** recover so a reaped abandoned request isn't wastefully re-serviced. **(3) Honest count:** the smoke test now collects its own pong (so a bootstrap leaves the maildir clean), and `status` reports "N awaiting service · M awaiting collection" (was Phase 1's honest-but-historical "delivered"). **Safety (validated):** a GC'd ULID can never reappear in `outbox/new` (unique ids; a retry uses a new id) so dedup/recover never re-execute a reaped pair (idempotent verbs are a backstop); and a response reaches `inbox/cur` only AFTER the agent fully read it, so GC never deletes a response before it's read. Verified: unit (gc prune-collected / leave-uncollected / retention-reaps-only-old / recover-skips-collected / idempotent; popo auto-collect + `collect` verb + best-effort failure; `Config.Retention`) + functional `TestMaildirGC` (real serving Popo: bootstrap leaves `inbox/new` empty, repeated pings keep it at 0, an old synthetic response is reaped while a recent one survives) + the full gate. **This closes all nine of the original observability symptoms.** Remaining maildir work and the queue-concurrency epic are separate, later | A status field that grows monotonically is worse than useless — it trains the operator to ignore it. The lever was recognizing `inbox/cur` was *already* the done-marker (dedup reads it), so the whole fix is a collect (rename) + a prune, with zero change to the at-most-once protocol. Making **popo** own the collect (not the agent hand-rolling file ops) keeps the verb interface consistent and saves agent tokens, with the raw rename documented only for the no-exec fallback. The one subtle call is the retention clock: keying it on `CompletedAt` (delivery) rather than request age is what makes a 1h TTL safe next to a 30-minute install — the uncollected window is seconds, the install minutes, and the two must not be conflated. Reaping the *pair* (not just the stale response) is what keeps recover honest |
| 72 | **Observability Phase 4 — agent capabilities reported (closes the last "status lies" symptom #8).** The status panel's "agent:" line always read "(not reported)": `collectStatus` read `<root>/protocol/capabilities.json` but **no production code wrote it** (only tests hand-wrote a weak, meaningless `{has_exec, has_file_write}`). Fix: a real, shared self-report. **(1) Schema** — `wire.Capabilities{schema_version, written_at, host{os,arch,libc}, agent{name,display_name,version,auth_configured}}` with a `Summary()` that renders the status line (`Claude Code 1.2.3 · auth ✓ · linux/arm64 (glibc)`, or `no agent yet · <host>` pre-install); the old placeholder booleans are gone. **(2) FS helpers** in `protocol` (re-exporting the wire type, like `Request`): `ReadCapabilities` (missing/unreadable → nil → "not reported") and `WriteCapabilities` (**read-modify-write**, atomic tmp+rename like `WriteHeartbeat`). **(3) Two cooperating writers** that never clobber each other — **bootstrap** (`provision`) writes the **host** block from the probe fingerprint (so status shows the real platform even before any agent), and **agent install/wrap** writes the **agent** block via a shared `recordAgentCaps` helper wired into the CLI `agent install`/`agent wrap` commands and the console's agent action; read-modify-write means re-bootstrapping never erases the agent block and installing an agent never erases the host block. popo (the static no-network client) is deliberately not involved. The reader (`collectStatus`) swapped the inline anon struct for `ReadCapabilities(...).Summary()`. Verified: unit (no-clobber round-trip incl. re-bootstrap-preserves-agent; missing-file read; `Summary`/host-string variants; `collectStatus` with/without the file) + functional (`skill_test` now asserts bootstrap-written host facts in `status` instead of a hand-written file; `TestConsoleOps_AgentWrap` asserts the wrapped agent is recorded and the host block survives — both passing on the VM). **Eight of the original nine observability symptoms are now closed; #6 (a real "unread" count) remains** deferred to Phase 2 maildir GC (**shipped — see #73**), along with the queue-concurrency epic and C6 conda (**shipped — see #74**) | "(not reported)" is the same class of dishonest status Phase 1 set out to kill — a panel field with a reader and no writer. The real lever was choosing a *useful* schema (the agent's identity + the host platform) over the placeholder booleans, which every working sandbox trivially satisfies and which carry no signal. Splitting the write across bootstrap (host) and install (agent) puts each fact where it's actually known, and the read-modify-write is what lets two independent writers share one file without a coordination protocol — the same "derive from authoritative state, written by whoever owns the fact" discipline as the rest of the observability arc. Keeping the schema in the leaf `wire` package (with the FS helpers in `protocol`) mirrors the existing envelope split and leaves the door open for popo to self-report later without a dependency tangle |
| 71 | **Observability Phase 3 — request lifecycle / in-progress visibility (1:1 receipt, both ways, + agent transfer progress).** Phase 1 made *status* truthful but a request was still invisible from pickup until completion — the dispatcher fires its single `Observe` only after the response is delivered. This phase shows work **as it happens**, resolving symptoms #1 (1:1 receipt/in-progress both ways), #2 (agent-initiated ops not showing in-progress on Popo), #3 (transfer times not reflected). **(1) Pickup hook** — a sibling `Dispatcher.ObserveStart(StartEvent)` fires the moment a request is read from `cur/` in `serviceCur`, so it covers both `drainNew` and `recover`, and agent- and operator-initiated requests alike (the dispatcher doesn't distinguish origin); `StartEvent` (request file name + the parsed `Request`) stays free of logging coupling, like `ServiceEvent`. **(2) Live-only event** — a new `activity.KindStarted` is pushed to the console channel but **never written to the durable JSONL** (in-progress is a "right now" signal; keeping it off-disk avoids orphan-started lines corrupting `Counts`/seed on a mid-flight restart); `Counts`/`eventBody` ignore it defensively. **(3) Serving indicator** — the console renders a pinned `▸ servicing <type> · <elapsed>` banner under the header, in its own `serving` field (separate from the operator `running` field, so an agent request and an operator action can show in-flight at once without clobbering the footer/keybindings); latest-wins pairing (safe — the dispatcher services one at a time): `KindStarted` arms it, the matching `KindServiced`/`KindDenied` clears it, and a `ClearServingMsg` covers the gate-deny case (no serviced event follows). A single `spinning`-guarded tick loop animates both indicators so they don't double-spin. Headless `serve` prints a `▸ <type> …` receipt line at pickup. **(4) Agent transfer progress (#3)** — `buildRegistry` had a deliberate `progress.Func == nil` for every install handler ("no operator surface"); now a Func is threaded through, and the console wires one that pushes `ProgressMsg` so a Nana-driven transfer lights up the serving banner's byte bar/%/ETA (routed by what's in flight: operator action → footer meter, else agent request → serving banner); headless/bootstrap pass nil. Verified: unit (`ObserveStart` fires at pickup for drainNew+recover and without a terminal observer; `KindStarted` ephemeral — a started+serviced cycle leaves exactly one serviced line on disk; `Counts` ignores started; applyEvent set/clear + gate-deny clear; serving+running coexist in one `dashboard` frame; agent progress routes to `servingProg` not the footer) + a serving teatest + the full gate. **Deferred (unchanged):** #8 capabilities written (Phase 4, separate); Phase 2 maildir GC; the queue-concurrency epic | An operator watching the agent build needs to see the work land 1:1, not just a delayed completion line — "is it stuck or working?" is unanswerable from a terminal-only event model. A *sibling* start callback (not a widened `ServiceEvent`, not in-handler logging) keeps the dispatcher a clean protocol primitive. Making `started` ephemeral is the key call: the value of in-progress is entirely live, and writing it would double the log and reintroduce exactly the seed-corruption class Phase 1 just fixed. A dedicated `serving` field (not reusing `running`, which gates keyboard input and owns the footer) is the minimal correct way to let agent and operator in-flight overlap. And the one-line `nil`→`Func` in `buildRegistry` was the whole of #3 — the progress machinery already existed end-to-end (`python/push.go` already wrapped its reader), it just had no sink on the agent path |
| 70 | **Observability Phase 1 — truthful status (heartbeat liveness, honest counters/queue/runtime), from authoritative state.** Nine operator-reported symptoms traced to: a terminal-only event model, no maildir GC, liveness conflated with the SSH link, status reporting intent-not-reality, and counters tallying UI-seen events with bugs. Phased fix, truthful-status first, derived from authoritative state. **(1) Background heartbeat ticker** — the heartbeat was written at the top of the single-threaded `Serve` loop, so a long install or a blocked approval gate starved it (seq stalled → nana saw a "dead" Popo and the console stayed green). It now runs on its own goroutine in `Serve` (sole `seq` writer, its fs write rides a separate concurrency-safe SSH channel), so liveness keeps advancing *during* a long handler; the redundant approver `keepalive` (a second racing writer) is gone. **(2) Dual header** — the console showed one green "● serving" driven solely by the SSH link, so a connected-but-wedged dispatcher still read as serving. Now it shows the **link** (● connected / ◌ reconnecting…) *and*, separately, **heartbeat health** (serving · hb N / ⚠ stale Ns) via a new `Dispatcher.OnHeartbeat`→`HeartbeatMsg`. **(3) Authoritative counters** — served/approved/denied seed from the durable activity log at startup (cumulative per sandbox, survive restart), a gate `operator_denied` no longer double-counts as serviced (`CodeOperatorDenied`/`isOperatorDenied`), and a remembered "deny all" no longer re-logs on every auto-applied request (symmetric with remembered-approve). **(4) Truthful status** — serviced events now render elapsed `DurMS` (the recorded-but-unshown total time) in `[POPO]`/`logs`/headless; runtimes are **health-probed** (`<bin> --version` exit 0 → ✓, else ✗) instead of reporting bare directory presence; the queue's misleading "unread" is relabeled "delivered" (the inbox isn't GC'd yet — Phase 2, **shipped: #73**). `collectStatus` was refactored to take `fs/runner/tree` so it's unit-testable. **Test-gap closure** (these shipped because nothing tested them): added unit coverage for `WriteHeartbeat`/seq + the starve-resistance regression, `collectStatus` (queue/runtime-health/caps), `activity.Read`/`Counts`, the live-`Console` approved/denied counters + seed, remembered-deny-logs-once, `isOperatorDenied`, and the duration render; the heartbeat-freshness teatest replaces the old one that locked in "serving follows the SSH link". **Deferred:** lifecycle/in-progress events (**shipped — see #71**); maildir GC (Phase 2 — a *real* unread count + pruning, reworking dedup off "response exists"); agent caps actually written (Phase 4); and true quick-op concurrency so pings never queue behind an install (separate queue-robustness epic) | An operator can't act on a dashboard that lies. The heartbeat starving behind the blocking loop was the load-bearing bug — it made liveness itself untrustworthy — so it's fixed at the root (independent ticker) rather than only displayed; and the header now separates "is the link up" from "is Popo actually working," which are genuinely different questions during a long install. Deriving counts/liveness/runtime-health from authoritative state (the durable log, the live heartbeat, an actual exec) rather than UI-seen events makes them correct-by-construction and restart-stable. Phasing keeps each change gated and reviewable; the heavier architectural roots (GC, lifecycle, concurrency) are sequenced behind the truthful-status wins they depend on |
| 69 | **Use pre-existing (brownfield) runtimes + per-request pip `extra_args` (foundation: discovery → source choice → system-Python venv → flag passthrough).** Every runtime was always relay-installed and `probe`/`Locate` only ever saw iceclimber's own `<root>/runtimes/…` — wrong for a corp VM that already ships a toolchain, and the agent could only send `{name, version}` so PyTorch-style installs (`--index-url`, `--pre`) were impossible. This increment lays the brownfield foundation: **(1) discovery** — `probe` now reports the `python3`/`node`/`java` on PATH (path + version) and which Python env managers are usable (`venv` only when both the venv module *and* ensurepip import — Debian ships python3 without a working venv otherwise; `conda` when present), as new `Fingerprint.Runtimes`; **(2) source choice + persistence** — a new `internal/runtimes` (Source/Sources + a JSON `Store` at `~/.iceclimber/<id>/runtimes.json` + `ParseFlag` + a pure precedence `Resolve`), a config `runtimes:` block, and a `bootstrap --runtime-source` flag; bootstrap detects → chooses (cmdline prompt **only** on an interactive terminal — headless defaults to `managed` and never blocks, per the headless-first-class rule) → persists; serve/install read it (config > persisted > managed); **(3) system Python via venv** — `python.EnsureEnv` resolves the interpreter for the chosen source: managed → `Locate` (unchanged); system → create/reuse an iceclimber-owned **venv at `<root>/envs/python-<minor>`** built from the system python, so installs land under `$ICECLIMBER_HOME` (off the PEP-668-locked, often-unwritable system site-packages) and relayed wheels are tag-matched to the *discovered* interpreter; the system minor must match the request (use what's there; **fail clearly, never mutate the system toolchain**); **(4) flag passthrough** — a shared `pkg.ValidateExtraArgs` (token-walk allowlist: each token an allowlisted flag — long/short, inline `=value` — or the value of a value-taking flag; **bare positionals rejected** so no smuggled targets/shell) gates per-request `extra_args`, forwarded to pip's index-facing commands (resolve, Tier-0 install, relay download, each shell-quoted; an agent `--index-url` wins over config and satisfies the Tier-0 "has index" check). Added a **glibc Lima sandbox** (Ubuntu; pre-installs system python3+venv/node/JDK) so brownfield + manylinux are genuinely testable, with the functional harness parameterized to target it (`requireGlibcSandbox`, `limaShOn`, name-carrying `sandboxConn`). Verified: unit (probe parse, `runtimes` precedence/round-trip/headless-default, `EnsureEnv` create/reuse/version-mismatch, allowlist accept/reject + command assembly) + functional — glibc box: probe sees the system toolchain, `--runtime-source python=system` persists, a C-extension wheel (markupsafe) relays + tag-matches into a venv and imports the pinned version from an isolated `pyvenv.cfg` env; musl box: `--tier mirror` with no config index installs only because the agent `--index-url` is forwarded (success = proof) and a disallowed `--pip-arg` is rejected. **Now also shipped (follow-ups):** the **PyTorch-on-glibc** end-to-end test (torch==2.4.1 from PyTorch's CPU index via `extra_args`, deps from PyPI, relayed + tag-matched into a system-Python venv, imported — the manylinux case musl can't do); a **console runtime-source modal** at bootstrap (with per-operation *lazy* source resolution + atomic store writes, so a choice takes effect live without a reconnect); and **agent install/wrap from the TUI** (the `nana` wrapper, token from the environment, never typed into the UI). **Deferred (designed-for, not built):** a `conda` manager + strategy — air-gapped conda needs relayed packages, a large lift revisited on concrete demand; venv covers brownfield Python today (**shipped — see #74**, both Tier-0 and the air-gapped relay) | A locked-down corp sandbox often already has the org's vetted toolchain; forcing a second relay-installed runtime is wasteful and sometimes wrong (the agent should match the box). The crux choices: a **venv** (not `--break-system-packages`, not system site-packages) is the one Python install target that works on a modern PEP-668 distro without root and stays under `$ICECLIMBER_HOME`; tag-matching relay to the *discovered* interpreter is what makes "ship packages that work with it" true; and an **allowlisted** `extra_args` (vs arbitrary passthrough) buys PyTorch-class installs without handing the agent a shell — bare-positional rejection is the load-bearing guard. Detect-and-offer at bootstrap (with a flag/config for headless) keeps the operator in control of environment policy rather than auto-preferring a possibly-stale system runtime. Shipping the foundation first (discovery → choice → venv → flags) de-risks the heavier conda/PyTorch work that builds on the same seams. **Security stance on `extra_args`:** the allowlist is deliberately narrow — index/version-selection flags, no build-behavior flags (`--no-binary`/`--no-build-isolation` excluded: they force sdist setup.py exec and conflict with relay's `--only-binary`) — and bare positionals are rejected so it can never become a shell. The one consciously-accepted surface is that in **relay** mode an agent `--index-url` steers the *controller's* `pip download` to an agent-chosen index (not covered by the web.fetch egress gate); that's the PyTorch capability, mitigated by the narrow allowlist + the hardcoded `--only-binary=:all:` on the download (no controller-side build exec) + offline install in the sandbox. System mode is **python-only** for now (node/java `source: system` is rejected at flag/config/prompt rather than silently no-op'd at install) |
| 68 | **SSH keepalive + supervised auto-reconnect — serve survives a dropped link (headless + TUI).** In corporate networks `serve` dropped frequently and needed a manual restart. Two confirmed root causes: (a) **no keepalives** — `remote.Dial` set neither TCP keepalive nor SSH `keepalive@openssh.com`, so during idle windows (a 30s–2min controller-side runtime download, a slow `web.fetch`) a firewall/NAT/bastion silently dropped the mapping; (b) **`Dispatcher.Serve` returned on the first transport error**, so one blip ended serving (and the console header still showed a green `● serving`). Fix, in three parts. **Keepalive (`remote`):** `Dial` starts a goroutine pinging `keepalive@openssh.com` every `ssh.keepalive_interval` (default 20s; negative disables) and **closes the client after a few consecutive misses** so the *next* fs/Run op fails fast instead of hanging on the OS TCP timeout; also sets TCP keepalive on the direct conn (the SSH ping additionally rides a ProxyJump tunnel, where TCP keepalive can't). Universal (every dial, incl. one-shot installs — their download idle window benefits). **Supervisor (`cli`):** `Serve` is left unchanged; a `runSupervisor` loop (factored for unit tests) opens a session, serves until it returns, and — unless ctx was cancelled (clean stop) — reconnects with capped exponential backoff (1s→30s, reset on a healthy cycle), **retrying forever**. The dispatcher is rebuilt per (re)connect (it snapshots fs/runner), and its existing `recover()` re-services any request stranded in `cur/` on the fresh connection — and the durable maildir means a request delivered *during* the outage is serviced once the link returns. Both **headless** (`serve`/`runHeadless`, logging reconnect lines) and the **TUI console** run the same loop; the console hands the live session from the supervisor to `consoleOps` + the agent.log bridge via a mutex-guarded `sessionHolder` (an operator op attempted mid-drop simply errors and is retried), and the header now shows real state via `tui.ConnStateMsg` — `● serving` when up, `◌ reconnecting…` while down (the passive `tui` viewer keeps `○ viewing`). **Password reconnect (`remote.CachingPrompter`):** a single in-memory cache is threaded through every dial so the operator types the password once — staged as *pending* on entry and **Committed only after a dial authenticates** (so a wrong first entry is re-prompted, never cached), reused silently on reconnect, and **Forgotten on an auth failure** (`IsAuthFailure`) so a stale/changed password re-prompts. The secret is never written to disk, never logged, gone at process exit; keyboard-interactive is **never** cached (it may be a one-time OTP/2FA code). Verified: unit (supervisor backoff/classification/cancel-stops + cap; `CachingPrompter` two-phase + Forget + Raw under `-race`; `IsAuthFailure`; keepalive-interval defaulting/disable; `ConnStateMsg`→header teatest) + the **functional killer test** (`serve` services a ping, `rc-service sshd restart` drops the live connection, a ping delivered after the drop is still serviced — no manual restart) + the full gate | Keepalives were the suspected "basic thing," and they do double duty: they keep the link warm *and* turn a silent drop into a fast-detected one (closing the dead client) so reconnect is timely instead of waiting out the multi-minute kernel TCP timeout. Rebuilding the session in a supervisor (rather than swapping a field inside a live dispatcher) keeps `Serve`'s contract — and the protocol package + its tests — untouched, and dodges the `TreePusher` capability-check a forwarding-FS would trip; `recover()` + the durable maildir make resume correct for free. Retrying forever with a cap (never self-exit) is right for an operator tool meant to sit and serve. Caching the password with two-phase commit honours "only a credential that worked is reused" while keeping unattended-but-terminal-launched reconnects silent; scoping the cache to password (never keyboard-interactive) avoids replaying an OTP, and memory-only/never-on-disk keeps the secret handling as strict as #65 |
| 74 | **C6 — conda as a first-class Python env_manager + `conda.install` (Tier-0 *and* air-gapped relay).** Closes the conda gap #69 deferred ("air-gapped conda needs relayed packages, a large lift"): a sandbox that ships conda and developers who expect `conda install -c conda-forge …`. **(1) Env seam:** `probe` now surfaces the conda binary (`RuntimeInfo.CondaPath`/`CondaVersion`, discarded before); `python.EnsureEnv` dispatches `EnvManager` `venv|conda` (was a hard reject), conda creating/reusing an iceclimber-owned env at `<root>/envs/conda-python-<minor>` (single source of truth `python.CondaEnvPrefix`). **This alone makes `pip.install` work *into* a conda env.** **(2) Selection everywhere venv is:** config `runtimes.python.env_manager: conda`; flag `--runtime-source python=system:conda` (`ParseFlag` `mode:env_manager`); the bootstrap cmdline prompt (venv|conda sub-prompt when conda detected); and the console modal (`RuntimeChoice.EnvManagers` → `bootstrapForm` Select → `RuntimeSelection.EnvManager`). **(3) `internal/conda` (mirror `internal/pip`, reusing `pkg.Outcome`/`ValidateExtraArgs`):** conda solves an env *holistically* (one `conda install -y --json -p <env> [-c ch] <specs>`, match-specs single `=`), parsed from `--json`; a solve failure is per-spec `Failed`, not a hard error. `extraArgAllow` is narrow — channels (`-c`/`--channel`/`--override-channels`) + the offline flags (`--offline`/`--use-local`) — no build/solver flags, bare positionals rejected. **(4) The relay tier (`relay.go`, the crux):** for an air-gapped box, the **controller's** conda (config `controller_conda`, default `conda`; the drop-in `mamba` works) runs a `CONDA_SUBDIR`-pinned `create --dry-run --json` solve for the sandbox platform, the controller downloads every resolved package over HTTP (sha256-verified), **synthesizes `repodata.json` per subdir from the solve records** (no `conda-index` dependency; `.tar.bz2`→`packages`, `.conda`→`packages.conda`; `depends: null`→`[]`), pushes a self-contained `file://` channel in via `remotefs`, and the sandbox creates the env **offline** (`conda create --offline --override-channels -c file://<chan>`, **forcing `--solver classic`** — conda's default libmamba solver can't load a synthesized local repodata for a file:// channel). Wired end to end: `conda.install` verb (`registry.condaDeps`), popo `conda.install` verb + help, CLI `install conda --python --tier --conda-arg`. **(5) Test:** miniforge provisioned on the glibc box (binary only, **no channel/pkg-cache seeding** — the relay pushes everything, replacing the brittle pre-seed offline idea); functional `TestCondaRelayEnvAndInstall` proves the whole air-gapped path — controller solve → 13-package download → repodata synth → channel push → offline env create → `import six` — **validated live with mamba 2.6 as the controller conda**. Unit: repodata synthesis (split/nil-depends), record merge/backfill (LINK ← FETCH), channel-arg filtering (offline dropped from the controller solve), subdir mapping, offline-create assembly, conda installCmd/extra_args/params, EnsureEnv conda branch. Skips cleanly when the box lacks conda or the controller lacks conda/mamba | venv covers brownfield Python, but conda is its own ecosystem (native deps, conda-forge) that developers on a conda box will reach for — refusing it forces them off the tool. The design reused every pip seam (the neutral `pkg` types, `remotefs` push, the relay shape, the `EnsureEnv` venv branch as template), so the marginal cost was the conda-specific solve/repodata logic, not new machinery. The relay's crux was **synthesizing a conda-installable channel from the solve JSON alone** — proven achievable against real mamba output (the `--dry-run --json` LINK/FETCH records carry every field repodata needs), which also made the functional test *hermetic* (no sandbox network) instead of depending on a fragile pre-seeded cache. The one hard-won detail: conda 26's libmamba solver silently fails to load a plain local `repodata.json` for a file:// channel — the classic solver (always built in) reads it fine, so the offline create forces it rather than shipping a zstd-compressed `repodata.json.zst`. Building **both** tiers (not just Tier-0) was a deliberate completeness call — the air-gapped case is exactly where a controlled sandbox lives, and the relay doubles as the test harness. Controller-conda dependency mirrors pip-relay's controller-python; absent, the tier errors/skips clearly. **Security stance (from the 4-dimension review):** an agent-chosen `-c` channel steers the *controller's* solve+download (a controller SSRF surface, outside the web.fetch egress gate) — consciously accepted, the exact analogue of pip's `--index-url` (#69), and bounded by: the controller solve is `--dry-run` (conda executes no package code resolving metadata), solve-record `fn`/`subdir` are `path.Base`-sanitized + validated before any filesystem write (a hostile repodata can't traverse out of the transient channel dir), package URLs must be https, and the sandbox install is `--offline --override-channels` against only the pushed `file://` channel (the agent's channel never reaches the sandbox install). The review also caught + fixed a relay idempotency bug (`conda create` refuses a populated prefix → use `conda install` when the env exists) |
| 79 | **Egress-proxy default hardening: tunnel-retry, supervisor backoff fix, host-agnostic package-artifact deny, and glob validation.** Follow-ups that protect + tighten the now-default proxy mode. **(A1) Reverse-tunnel retry:** every `serve` opens sandbox port 18080 via `ssh -R`; a restart before the peer releases the forward gave `tcpip-forward request denied` and a fatal startup. `startEgressProxy` now retries that specific race (bounded linear backoff, ~2.5s) and, on exhaustion, returns a clear "another serve may be holding the port" message. **(A2) One-shot maven:** proxy-mode `maven build` needs an active `serve` (the proxy only listens then); a one-shot build now detects the proxy-unreachable failure and says so (use serve, or `egress_mode: relay`). **(A3) Supervisor backoff bug:** `runSupervisor` reset backoff on `connected`, but `connected` only meant "dial authenticated" — a proxy-startup failure returned `(connected, err)` and reset backoff to 1s *every* iteration, hot-spinning forever and dispatching nothing. `cycleFunc` now returns `(authenticated, served, err)`: Commit the password on `authenticated`, but reset backoff only on `served`, so a persistent proxy failure escalates 1s→30s with a clear message instead of spinning. **(B1) H1 residual closed host-agnostically:** a package deny blocks the index (stopping normal installs) but the wheel lives on a name-less content-hash CDN (`files.pythonhosted.org`, or a custom index's own CDN) that a URL glob can't package-scope — the bypass. Closed by binding the deny to the **exact artifact URLs the package's index lists** (`egress.ParsePackageIndex` — PEP 691 JSON + PEP 503 HTML — resolved against the index host from the deny glob, so no CDN is assumed): (a) a startup/per-cycle **resolve** fetches each denied package's index (controller-side) and pre-denies its artifacts — for packages denied before serve, whose index is 403'd and never served; (b) **learn-on-serve** (new `proxy.SetResponseObserver`, which buffers only small index responses ≤4 MiB and streams everything else) records artifact→index for indexes served while allowed, so a mid-session deny instantly blocks already-seen artifacts. The PathDenier denies a URL if `StoreDenied` OR it's a resolved/learned artifact of a denied package; artifact URLs and request URLs are matched through one shared `normalizeEgressURL` (port/case/dot-segment/slash/query canonicalized). npm/maven carry the name in the artifact path, so a `…/<pkg>/*` glob already covers them. **(B2) Glob validation:** `egress.ValidateHostPattern` (allowed_domains are host-only — reject a scheme/path footgun that silently never matches) + `ValidateURLGlob` (store rules match the full URL — require a scheme), wired into `config.validate` and `Store.AddAllow/AddDeny`. | The default flip (#78) made the tunnel-restart race and the supervisor hot-spin real regressions, not corner cases — so retry + the authenticated/served split are the load-bearing fixes for a default anyone gets. The H1 close is the interesting one: the naive fix (a filename glob on `files.pythonhosted.org`) hardcodes one CDN and misses packages served from other repositories — the exact hole to avoid — so the deny is instead bound to the URLs the index *itself* names, wherever they point, which also handles custom indexes for free. Two mechanisms are needed because the timing differs: a denied package's index is 403'd (never served → can't be learned → must be resolved up front), while a mid-session deny of a package already fetched is caught by the served-index record without a reconnect. Buffering is confined to small index bodies so the streaming guarantee for large artifacts holds. The validation closes a quiet footgun (a URL in `allowed_domains`, which matches host-only, silently never matches) that the flip made more likely as more configs are written. |  With full parity banked and the proxy hardened (#77), `egress_mode` now defaults to **proxy** — `EgressProxy()` is true unless an explicit `egress_mode: relay` opts out (`EgressRelay()`). A fresh config gets the native-tool path: the sandbox's own managers reach registries through the controller MITM proxy over the reverse tunnel, with package/path-level policy; popo install verbs still relay (unaffected by the mode), so the default experience is unchanged for them. Relay stays first-class for the compliance story where the sandbox must open **no connection at all** (only relayed files). **Ripple handled:** most functional tests use popo verbs (relay-tier, mode-independent) or a `serve` whose idle proxy is harmless, so they pass unchanged under the new default; the two spots that assert *relay semantics* — the `maven build` scenario and the air-gapped acceptance demo — are pinned `egress_mode: relay` (the proxy-mode online `maven build` has its own coverage in `TestProxyMavenBuild`). **Sharp edge (documented):** proxy-mode `maven build` — and any native-tool egress — needs the proxy *listening*, which only happens during an active `serve`; a one-shot `maven build` with no serve fails in proxy mode (use relay, or run it under serve). The agent path (Nana inside serve) is unaffected. | The whole arc's premise (#76: "if it works, revert to this as our primary method") is now met: parity is proven across pip/npm/conda/maven + git, policy is finer than relay's, and the failure modes are closed (#77). Flipping the default is the honest expression of "proxy is primary" — a new user gets the more compatible native-tool path, and the strict air-gap is a deliberate opt-in rather than the baseline. Keeping relay first-class (not deprecated) preserves the one thing proxy can't offer — a sandbox that literally never connects — which some regimes mandate. The maven-needs-serve sharp edge is inherent (the proxy is a serve-lifetime resource); documenting it + pinning the two relay-semantic tests is cheaper and clearer than making one-shot commands spin up a throwaway tunnel. |
| 77 | **Egress-proxy productionization — streaming, package/path-level policy, and full pip/npm/conda/maven parity (PX6–PX9).** Hardens #76's proxy from "spike + pip/git" toward a primary-capable path. **(1) Streaming (PX6):** the proxy no longer buffers whole response bodies (a large-artifact OOM + a hang re-serializing HTTP/2 over the tunnel) — it re-frames upstream responses to HTTP/1.1 with explicit Content-Length passthrough or chunked framing, streaming the body, and drops stale Transfer/Content-Encoding. **(2) Package/path-level deny (PX8):** the host is admitted at CONNECT (domain-level), but a per-request `PathDenier` re-checks the full URL against the egress store, so a deny glob carrying a path (`https://pypi.org/simple/six/*`) 403s just that package on an otherwise-allowed registry — the granularity the domain-level relay never had. The wiring matches a *port-free* URL (`https://`+host+path) because the request URL carries `:443` that deny globs are written without (the bug that silently no-op'd the veto). **(3) Parity (PX7):** functional tests prove the sandbox's OWN native npm (musl) and conda (glibc) egress through the proxy via `popo shellenv` alone; **Maven** — the one ecosystem the CA env vars don't cover (the JVM ignores them) and with no native sandbox mvn — gets `maven build` running **online through the proxy** (`tier:"proxy"`): push the Maven tool, build a PKCS12 JVM truststore from the egress CA via the sandbox JDK's keytool (`java.EnsureEgressTrustStore`, idempotent; built eagerly at java-install on both the CLI and agent paths, and by the build), and run `mvn` online with `-s maven-settings.xml` (no controller Maven, no offline `.m2`). `egress-env.sh` also gains guarded `JAVA_TOOL_OPTIONS` (truststore) + `MAVEN_ARGS` (`-s`) so any hand-run JVM/mvn egresses too. **(4) Quality-pass hardening (4-dimension review):** the path-deny match is *normalized* before matching (real scheme, canonical host — lower-cased/port-free/no-trailing-dot, `path.Clean`'d path with dot-segments + duplicate slashes collapsed and trailing slash preserved, query appended) so an upstream-collapsed path (`/simple/./six/`, `//six/`, a decoded dot, a cased host, `:443`) can't evade a rule; TLS leaves are minted for the **admitted CONNECT host, not the client's SNI** (SNI is attacker-chosen — honoring it was a per-name RSA-keygen CPU lever the old comment wrongly called closed); **HEAD** responses are framed body-less (the declared Content-Length with no body would otherwise hang the client + desync the tunnel); the MITM server sets a **TLS 1.2 floor** + **idle read deadlines** (slowloris/goroutine-hold cap); and the Java truststore is built via **temp-file + atomic rename** (a half-written store never gets trusted by the `-f` guard). **Known limit (documented, not a bug):** a path-deny glob scopes an index/metadata page — enough to stop the normal resolve→install — but a broadly-allowed artifact CDN whose paths are content hashes, not names (`files.pythonhosted.org`), can't be package-scoped by a URL glob; host-level allow/deny still applies there. | The spike (#76) proved reachability; productionization is about the failure modes a primary path must not have: unbounded memory on big artifacts (streaming), and policy that stops at the host when the proxy can already see paths (package/path deny). Maven was the load-bearing parity gap — it's the ecosystem where the proxy's "any tool, config only" thesis is weakest (JVM trust + no native mvn), so closing it (online build + auto truststore) is the strongest evidence that proxy can be primary; documenting *how* fiddly Java is (keystore, not env var; settings `<proxies>`, not JVM props) is itself part of the parity result. Path-level matching on a normalized port-free URL is the kind of quiet bug that in-process tests miss (they check `r.Path`, not the port-bearing `r.URL`) but a live install exposes immediately — caught by validating six-blocked/idna-allowed against a real `serve`. Relay stays the default until the proxy-primary call is made with this parity banked |
| 76 | **Egress-proxy mode — the sandbox's own package managers reach real registries through a controller MITM proxy over the SSH tunnel (an additive alternative to relay).** A second way for the sandbox to get packages, opt-in via `egress_mode: proxy` (default stays `relay`). Motivation: the relay integrates each manager explicitly and reimplements resolution (conda repodata synthesis, maven `go-offline` priming, npm `ci` staging); a proxy runs the *native* tool against the real registry, so lockfiles / transitive + plugin resolution / download-during-build "just work," and **any** HTTP(S)-honoring tool works with **no per-tool Go**. The air-gap is preserved because the proxy runs on the controller and is exposed to the sandbox via an **`ssh -R` reverse forward** (`remote.SSHRunner.RemoteListen`) — the sandbox's only egress is a loopback port over the *existing* SSH channel; it has no direct internet. **(1) `internal/proxy`** — a persistent per-sandbox CA (private key stays on the controller) + a MITM proxy that mints per-SNI leaves, terminates TLS, forwards upstream over real TLS, and — the hard-won spike fixes — handles the tunnel conns manually (a naive `http.Hijack` drops the buffered ClientHello), normalizes upstream **HTTP/2 → HTTP/1.1** (else the client rejects the `HTTP/2.0` status line), and strips `Accept-Encoding` (gzip-framing hazard). **(2) Lifecycle** — `serve` (both the supervised loop and one-shot) brings the tunnel + proxy up per (re)connect and tears it down at cycle end. **(3) No-root trust** — because *all* egress is MITM'd, the sandbox only ever sees our-CA-signed certs, so it trusts **just our CA**, installed under `$ICECLIMBER_HOME` with no system-store write: bootstrap writes `certs/egress-ca.pem` + `egress-env.sh` (HTTPS_PROXY + each ecosystem's TLS knob — `SSL_CERT_FILE`/`REQUESTS_CA_BUNDLE`/`PIP_CERT`/`CURL_CA_BUNDLE`/`GIT_SSL_CAINFO`/`CARGO_HTTP_CAINFO` for OpenSSL tools, `NODE_EXTRA_CA_CERTS` for Node) + `maven-settings.xml` (Maven routes via `settings.xml <proxies>`, not JVM props; its keystore is built when a JDK is present); `popo shellenv` sources `egress-env.sh` so tools pick it up automatically. **(4) Policy + approval + audit** — every request is gated through the existing egress `Policy` (store Deny > store Allow > config `allowed_domains` pre-allow > interactive approver > deny), per-host memoized so an install's many requests prompt at most once, with a headless Hold denying (a live connection can't defer to the async pending queue); each request is audited with its verdict. Validated: the spike proved **pip, npm, conda, and `mvn package`** all install through the proxy against real registries with **full per-artifact URL visibility** (so package/path-level policy is on the table for every one); production functional tests then proved pip parity in real `serve` + an unlisted-host deny, and **`git clone`** — a tool with zero iceclimber integration — working through the proxy. **Relay stays the default and the parallel path; "make proxy primary" is revisited once broader parity is banked.** | The relay's per-manager integration was real, fragile work (repodata synthesis, offline-repo priming) that reimplements what the tools already do; the proxy trades that bespoke Go for one chokepoint + per-tool *config*, and lets the sandbox run the real toolchain. The load-bearing insight is that the SSH reverse tunnel keeps the "sandbox has no internet" property intact while still letting native tools reach registries — so proxy mode is not a weakening of the air-gap, just a different (and more compatible) way to mediate it. Terminating TLS with a controller CA is what turns the proxy into the ideal policy/audit chokepoint (full URLs, so package/path granularity is available where the domain-level relay never saw paths), and doing the trust install per-tool under `$ICECLIMBER_HOME` (no root, no system store) is what makes it deployable in a locked-down sandbox — the one genuinely fiddly ecosystem is Java (its own keystore + `settings.xml` proxy, not the JVM proxy props most guides cite). Keeping it additive behind `egress_mode` (default relay) means neither path regresses while parity is proven; the two coexist, and the strict "no live egress at all, only relayed files" story that some compliance regimes want remains available in relay mode. Security posture inherits `web.fetch`'s: agent-influenced destinations are gated by the same operator policy, now per-URL |
| 75 | **Manifest-driven installs (`npm --project`, `conda --file`), the Maven build tool in the sandbox (air-gapped `mvn -o package`), an idempotent-symlink relay fix, representative stress scenarios, and Nana durability guidance.** Follow-on to #74, driven by "build full-fledged projects with each ecosystem's own tooling." **(1) npm manifest install** (`internal/npm/project.go`): `install npm --project <dir>` / `npm.install project:"<dir>"` installs a whole `package.json` — relay reads the manifest (+ lockfile → `npm ci`) from the sandbox, the controller's npm resolves, and `node_modules` is relayed beside it (local `./node_modules` resolution, no `NODE_PATH`); a mirror variant runs the sandbox's own npm in the project dir. **(2) conda `environment.yml`** (`internal/conda/manifest.go`): `install conda --file <env.yml>` / `conda.install file:"…"` builds the whole env at `<root>/envs/<name>` — parses channels + the pinned python + specs (a `pip:` subsection is rejected; version constraints like `numpy>=1.20` are preserved), relay reuses #74's solve→channel→offline-create, mirror is one holistic `conda create`. **(3) Maven build tool, air-gapped** (`internal/maven/build.go`): `maven build --project <dir> --java <feat>` runs `mvn` **in the sandbox** on a real `pom.xml` — the crux is priming an offline `.m2` on the controller by **resolving, not building** (`mvn dependency:go-offline` reading only the pom, so an untrusted pom can't execute lifecycle plugins on the controller), relaying the controller's exact Apache Maven version (SHA-512-verified) + the primed repo, then `mvn -o package` offline (javac on the sandbox's JDK). New `maven.build` verb + CLI + `controller_mvn`. **(4) Idempotent Symlink** (`remotefs`): plain SFTP `SSH_FXP_SYMLINK` fails on an existing name (`SSH_FX_FAILURE`), which aborted any re-relay or overlapping tree — now clear an existing symlink/file first (SFTP; a real dir is left, not destroyed) / `ln -sfn` (exec). This is what makes `blessed-contrib` (whose `node_modules/.bin` is all symlinks) relay reliably. **(5) Scenarios** reworked into representative stress tests: Python `pytorch`+`pandas`+`numpy` as a conda project via the air-gapped relay on glibc *and* `pandas`+`numpy` via pip relay (musllinux) on musl; Node a real `blessed`+`blessed-contrib` npm project rendering a headless blessed-contrib dashboard; Java a `pom.xml` Gson project built by `mvn` in the sandbox. Harness parameterized for the glibc box (`RequireGlibc`). **(6) Nana guidance** (`NANA.md`/`PROTOCOL.md`): everything goes through Popo — **never** reach the network directly or route around Popo (no fallback); a request is durable and may be left queued for Popo to serve later; be patient (a runtime/package pull can take minutes). Verified end to end on both boxes (torch matmul + pandas in the offline conda env; blessed-contrib loads from the relayed tree; `mvn -o package` → jar runs with Gson from the offline repo; pandas via pip relay) + unit (`-race`) + the full gate (functional, all scenarios, tui-functional, demo) + a 4-dimension quality pass | The bridge already installed *packages*; "full-fledged project" means the project's own manifest (`package.json`/`environment.yml`/`pom.xml`) drives the install, so each new mode reads the manifest the sandbox already holds and reuses the existing relay push — the marginal cost is a parser + a tool invocation, not new transport. The Maven build was the deep one: running a build tool to prime a repo would execute untrusted build logic on the trusted controller, so the design deliberately mirrors the rest of iceclimber's "resolve, don't execute" stance — `go-offline` (not `package`) on the controller, reading only the pom, is what keeps the prime safe; the sandbox (already untrusted) is where the real build runs. Pinning the relayed Maven to the controller's exact version removes super-pom plugin-default drift so the offline build finds every plugin the prime downloaded. The symlink fix was the load-bearing enabler for the whole "real npm project" story — the reported "blessed-contrib is hard over SFTP" was exactly this non-idempotent-`Symlink` abort, not a missing-symlink problem (the links relay fine; the peer `blessed` must simply be installed alongside). The Nana guidance closes the loop with the air-gapped model: an agent that "helpfully" falls back to the network defeats the sandbox, so the contract is stated plainly. **Controller-tool dependencies** (npm for #1 relay, conda/mamba for #2, Maven+JDK for #3) mirror pip-relay's controller-python — each skips/errors clearly when absent, and the relevant scenario skips |
| 67 | **Rename `$ROOT`/`ICECLIMBER_ROOT` → `$ICECLIMBER_HOME`; `agent wrap` + `popo shellenv`.** Three small QoL items. (a) **Rename:** `$ROOT` was ambiguous — hard rename (sandboxes re-bootstrap, so no back-compat) of the env var `ICECLIMBER_ROOT`→`ICECLIMBER_HOME` (exported by the launcher, read by popo's self-locating `resolveRoot`) and the doc shorthand `$ROOT`→`$ICECLIMBER_HOME` across skill docs/operator docs/comments **and** the test scripts' bash-local `ROOT` + `{{ROOT}}` brief template (so tests and runtime match); zero references remain. (b) **`iceclimber agent wrap [name] [--bin <path>]`:** drops the iceclimber wrapper (auth env + `run` + `nana`) around a binary **already on the sandbox**, **no relay** — for pre-baked images / out-of-band installs. `Install` and `Wrap` share the extracted `Installer.wire` (env/run/nana/verify); `Wrap` resolves the binary to an absolute path (`--bin`, else `command -v <Bin>` over the sandbox so the launcher bakes a real path, not a name resolved against the run shell's PATH; not-found → an error naming `--bin`) and `mkdir -p`s the agent dir. (c) **`popo shellenv`:** a local-only verb (no maildir) printing a `brew shellenv`-style eval block — `export ICECLIMBER_HOME` + `<root>` on PATH — so an interactive sandbox shell runs `popo`/`nana` by name; minimal, **no secrets** (the token stays in the 0600 env.sh); a tiny local `shellQuote` keeps popo a dependency-free static binary. Verified: unit (Wrap binPath resolution/no-relay/not-found over a real local ExecFS; shellEnvBlock + escaping; resolveRoot) + live functional (wrap `/bin/echo` → launcher bakes it, nothing relayed, `nana` runs it; `eval "$(popo shellenv)"` → popo resolves by name) + the full gate incl. demo | `$ROOT` reads as a generic placeholder; one unambiguous name across code, agent docs, and tests removes a real point of confusion. Decoupling the wrapper from the relay (`wire`) makes "an agent is already here, just wire it" a first-class path with zero new machinery — and resolving the binary to an absolute path at wrap time avoids the non-interactive-PATH trap. `shellenv` is the `brew` pattern operators already know; keeping it secret-free and print-only (no rc-file rewriting) keeps it safe to paste and opt-in |

---

## 12. Suggested build phases

**v1:**

1. ✅ **CLI skeleton + probe (fingerprint only, no installs)** — cobra surface (all §9 verbs present; unbuilt ones stub with their phase label), `config`/`init` real, `remote.Runner` SSH boundary (known_hosts-verified, configurable path), `probe` fingerprints OS/arch/libc(multi-signal confidence)/root-viability(real write tests)/existing-tree. Unit-tested at the Runner boundary **and verified end-to-end against a real Alpine (musl/BusyBox) sandbox** (see Testing strategy below) — probe's POSIX-sh commands and musl detection are proven on real BusyBox.
2. ✅ **Maildir protocol end-to-end with `ping`/`pong`, `RemoteFS` (both implementations) + conformance tests; delivery semantics** — `internal/remotefs` (SFTPFS + ExecFS, conformance at both layers), `internal/protocol` (envelope, `tmp/new/cur` maildir with atomic Deliver/PickUp, ULID names, Dispatcher with `id` dedup + `cur/` recovery sweep + counter heartbeat), real `serve --once`/`serve` and minimal `bootstrap` (tree + heartbeat + §7 smoke test). **Verified E2E on Alpine over both transports** — stdin-over-exec and BusyBox `mv` proven on real hardware. *(Deferred to later phases: `.popo.lock` contention, blob streaming, full re-bootstrap UX.)*
3. ✅ **`python.install` via python-build-standalone** — `internal/python` resolves an exact PBS build (`latest-release.json` + `SHA256SUMS`, sha-verified, pinned), extracts with the Go stdlib, and pushes the tree over `RemoteFS` (either transport; `Chmod`+`Symlink` added) then runs `bin/python3` to verify. `install python <minor>` (sync) + the `python.install` verb. **Verified on Alpine/musl** (3.12.13 in ~6s over SFTP, idempotent). *(Shebang rewriting deferred to phase 4; the optimized ExecFS bulk push later shipped — decision #55.)*
4. ✅ **`pip.install` Tier 0 (internal mirror, remote-exec)** — `internal/pkg` (manager-neutral types) + `internal/pip` (resolve via `--dry-run --report`, then per-package `--no-deps` install; tier=mirror + sha256) + `python.Locate`. `install pip <pkg>… --python <minor>` (sync) and the `pip.install` verb; `bootstrap` writes `pip.conf`. **Verified on Alpine/musl** vs PyPI: unversioned `requests` co-resolves 5 deps, each recorded; unknown package → `resolution_failed`. *(Tier 1 relay = phase 5, reuses the resolve stage.)*
5. ✅ **`pip.install` Tier 1 (relay)** — `internal/pip/relay.go`: controller cross-platform `pip download` (operator's python3) → relay wheels via `RemoteFS` → offline `pip install --no-index --find-links --report` in-sandbox (tier=relay, hashed). `--tier auto|mirror|relay`; config `controller_python` + `pip.controller_index_url`. **Verified on Alpine/musl:** a C-extension (`markupsafe`) downloaded cross-platform on macOS imports and runs on the VM. *(Cross-platform-download risk spiked & resolved; sdist-only → Tier 2, parked v2.)*
6a. ✅ **`web.fetch` — sandbox-exec venue + SSRF literal-IP floor + audit log** — `internal/webfetch` (curl/busybox-wget over exec, no Python; inline/blob body) + `internal/audit` (controller JSONL). `web fetch` CLI + the `web.fetch` verb. **Verified on Alpine/musl** (busybox wget HTTPS; large body → blob). *No exfil hole — sandbox's own egress only.*
6b. ✅ **`web.fetch` controller venue + egress gating (§6.1)** — `internal/egress` (rewrites, venue selection, allow/deny + pending stores) + `internal/webfetch` controller backend (`net/http`, SSRF-safe dial blocking private/link-local/metadata) + `pending`/`approve [--remember]`/`deny` + `serve --deny`. Audit gains `rewritten_url`+`decision`. **Verified on the VM:** rewrite re-venue (ungated), hold→approve→controller fetch, SSRF refusal, deny→egress_denied.
7. ✅ **`NANA.md` + `status` (v1 finale)** — `internal/skill` embeds the sandbox-side skill doc (request/response over the maildir, counter-based heartbeat liveness, absolute-path contract, all four verbs, capabilities self-report); `bootstrap` drops it to `skill/NANA.md`; `skill print`/`skill path`; `status` reports liveness + queue + runtimes + the agent's `capabilities.json`. **Verified on Alpine.**
8. ✅ **Acceptance demo — a real Claude agent in an air-gapped sandbox** (proves the *premise*, not just the mechanism) — `test/lima/demo.yaml` (provisioned Alpine + Claude Code on musl), `demo-firewall.sh` (egress dropped to only Anthropic's ranges), `test/demo/` brief + harness, `make demo` (headless CI gate, `//go:build demo`, subscription token, API key emptied) + `make demo-live` (operator approves the egress hold live). **Validated end-to-end:** the agent installed Python 3.12.13 + `rich` (both relayed by Popo), fetched `xkcd` through the gated controller venue, and ran a program rendering the comic; `make demo` green in ~97 s. See [`DEMO.md`](./DEMO.md).

**🎉 v1 is complete** — every phase above is implemented and verified end-to-end against a real Alpine/musl sandbox, and phase 8 proves the whole premise with a real Claude agent under a true air-gap. The post-v1 increments (Node/npm, Java/Maven, the operator console Phases 1+2) shipped on top; the `sandbox_id` namespacing invariant is now tested (`TestSandboxStateNamespacing`), and the pre-fleet hardening pass (#54) closed the remaining polish. What's left is **multi-sandbox** (Phase 3) + the v2 backlog.

**Post-v1 increments (shipped):** Tier 0→1 pip auto-fallback (#25); **Node/npm**, the second language (#44); the **observability TUI** (#45); the **TUI-first operator console** — bare `iceclimber`, serve embedded with inline modal approvals (#46, Phase 1); **operator-initiated installs + bootstrap** from the console (#47, Phase 2a); and **Java**, the third language — JDK runtime via the Adoptium API (#51) + JVM dependency resolution via Coursier (#52, Tier 0 mirror + Tier 1 relay).

**Console roadmap (incremental, #46–#47):** **Phase 2a (done, #47)** — install form + bootstrap confirm. **Phase 2b (done, #53)** — a live **status panel** (`s`: heartbeat/queue/runtimes/capabilities) and an **egress rules + pending** view (`e`: approve/deny/forget). **Phase 3 (next)** — **multi-sandbox** (a sandbox list/switcher; serve several at once — model state is already keyed per `sandbox_id`).

**v2 (demand-driven, see §0):** sub-agent router for `web.research` and Tier 2/3 fallbacks; Tier 2 build environment; true fleet multiplexing. *(`ExecFS` bulk-transfer shipped — #55.)*

### Testing strategy

Tiers, run at different frequencies (washu testing guide):

- **Unit** — fast, every change. Mock at the `remote.Runner` boundary; table-driven parsers/detection logic (`internal/probe`, `internal/config`). Run with `go test -race ./...`.
- **TUI flows** — fast, no VM, but driven through the **real Bubble Tea runtime** via `teatest` (`internal/tui/flows_test.go`): keys and messages go through the genuine event loop and huh form, asserting on rendered output + the operator actions that fire. This is the interactive analogue of the CLI's stdin/stdout tests. **Rule: every TUI flow — every key, modal, form field, and state transition — has a flow test**, so the console is confirmed end to end, not approximated. (huh advances fields via async cmd round-trips, so checkpoint each field's placeholder before typing.)
- **Functional (E2E)** — low-frequency, real dependencies. Black-box: the **real binary** drives a **Lima/Alpine** sandbox over SSH (`test/functional/`, `//go:build functional`, `make e2e`). The console's operator-action executor has its own white-box functional test (`make tui-functional`: install + bootstrap against the VM, asserting Nana's echo). Alpine = **musl + BusyBox** is deliberate — it enforces the "POSIX sh only, no GNU-only flags" discipline (§6, §7) and exercises musl libc detection. Each phase adds its functional test here (phase 2: maildir `ping`/`pong`; etc.), so "test as we build" holds. These run with **open internet** — they prove the mechanism, not the air-gap premise.
- **Acceptance (opt-in)** — the whole premise, end to end: a real Claude agent in an **air-gapped** sandbox (`test/demo/`, `//go:build demo`, `make demo`). Needs a Claude **subscription** token (`DEMO.md`); never part of `make test`. This is the only tier that simulates the no-egress boundary the design exists for.

---

## 13. Prior art — A2A (Agent2Agent), considered & not adopted

A2A (Google, now v1.0 / Linux Foundation, ~150 orgs as of 2026) is an open protocol for **independent, network-reachable agents to discover and delegate to each other**: JSON-RPC 2.0 over **HTTPS**, SSE streaming, push notifications, discovery via a signed **Agent Card** at a well-known HTTP endpoint.

**Not adopted for the Popo ↔ Nana link — transport mismatch with the project's defining constraint.** Every A2A assumption (an HTTP server, a reachable endpoint, a served Agent Card) is precisely what the architecture refuses to require: an **SSH-only** link, a sandbox with **no assumed network listener and no egress**, and a Nana whose only guaranteed primitive is **file read/write** (§2, §4.6). Adopting A2A would re-introduce the exact assumption the maildir-over-SSH design exists to avoid. This is a firm rejection, not a "maybe later."

**Borrowed concepts (no dependency taken):**
- **Agent Card → `capabilities.json` (§4.6).** Same capability-self-description instinct; loosely align vocabulary. Reinforces pruning `capabilities.json` to fields something actually consumes.
- **Task state `input-required` → `needs_clarification`.** Borrow the *naming* for legibility; do **not** adopt A2A's full task state machine (decision #8's "no pending stub, heartbeat-only" is simpler and stays).

**Possible v2 re-entry, controller-side only:** if the v2 `web.research` sub-agent (§4.5) ever delegates to external specialist agents, **Popo** (which has real internet) could act as an A2A *client* — outbound HTTP from the controller, never touching the sandbox link. Noted, not designed.
